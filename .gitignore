# Internal task tracking artifacts.
.dolt/
*.db
.beads-credential-key

# Mockingbird ignores

# Model downloads (multi-GB whisper.cpp / GGUF files; user fetches locally)
models/

# Tauri signing keys live outside the repo by convention (~/.tauri/), but
# if they ever leak in, refuse to ship.
*.key
*.key.pub
!.code_puppy/**/*.key.pub  # (defensive: never expected, but doesn't shadow)
*.pfx
*.pem

# Env / secrets
.env
.env.*
!.env.example

# Build artifacts
target/
node_modules/
dist/
build/
src-tauri/target/

# IDE / OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db

# Test / coverage
coverage/
*.profraw
playwright-report/
test-results/

# Python hook caches
__pycache__/
*.pyc
.pytest_cache/

# Logs
*.log
# ...but spike log fixtures under docs/spikes/ are intentionally committed
# (small JSONL evidence backing the findings docs that reference them)
!docs/spikes/**/*.log

# Internal capability notes (maintainer-only reference docs)
internal-capabilities-notes.docx

scratch/

# ---------------------------------------------------------------------------
# Maintainer-only artifacts.
#
# These files live on the maintainer's local disk and drive day-to-day
# development. They reference internal tooling and process notes that
# are intentionally hidden from the public repo to keep the published
# surface focused on the shipping product.
# ---------------------------------------------------------------------------

# Internal maintainer docs (hidden from public repo)
AGENTS.md
PLAN-mockingbird-v2.md
STATUS.md
mockingbird-activity-capture-plan.md
mockingbird-logo-motion.html
verify_iter1_schema.py
docs/LESSONS.md
docs/PRODUCT-STATE.md
docs/DATA_MODEL.md
docs/SETTINGS.md
docs/CONTRIBUTING.md
docs/adr/
docs/phases/
docs/judges/
docs/knowledge-graph/
docs/historical/
docs/archive/
docs/audits/
docs/reviews/
docs/spikes/
docs/cleanup/
docs/design/

# Internal R&D scratch (corpus + harness + iter logs)
experimental/

# Internal visual-regression baselines
playwright-results/

# Internal development tooling
.code_puppy/
.beads/
.beads-credential-key

# Internal command tooling
.agents/

