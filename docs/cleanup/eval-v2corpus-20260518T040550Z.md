# Mode-eval report — `v2corpus`
_Generated 20260518T040550Z by `mode_eval` bin (ADR 0024)._

## Methodology

- Pipeline: `Preprocessor` (preproc@v1) → DB prompt + dictionary + few-shot → `OllamaProvider` against `localhost:11434`.
- Fixtures: 52 cases across 18 categories (see `src-tauri/eval/baseline.json`).
- **Preservation** is automated (substring match on `must_preserve`). **Format-fit** and **register-fit** are human-judged from the diffs below.
- A mode hits **badass** when ≥100% preservation, ≥90% format-fit ≥1, ≥70% format-fit =2 (per ADR 0024). Numbers below give the human reviewer a quick read.

## Mode configuration

| Mode | Display | Model | Temp | Max tok | Prompt ver |
|---|---|---|---|---|---|
| `casual` | Casual | `qwen2.5:3b-instruct-q4_K_M` | 0.2 | 1024 | v2 |
| `normal` | Normal | `qwen2.5:7b-instruct-q4_K_M` | 0.1 | 4096 | v5 |
| `formal` | Formal | `qwen2.5:7b-instruct-q4_K_M` | 0.1 | 8192 | v2 |

## Summary

| Mode | N | Errors | Preserve avg | Full ✅ | Partial ⚠️ | Zero ❌ | avg LLM ms | avg total ms | max LLM ms |
|---|---|---|---|---|---|---|---|---|---|
| `casual` | 52 | 0 | 97.2% | 41 | 11 | 0 | 1233 | 1240 | 6466 |
| `normal` | 52 | 0 | 97.5% | 45 | 7 | 0 | 2789 | 2797 | 8430 |
| `formal` | 52 | 0 | 88.5% | 28 | 24 | 0 | 3332 | 3344 | 9215 |

## Per-fixture detail

### `01_enum_short` — verbal_enumerated_list (short)

- **Intent:** Shopping list with three items, enumerated.
- **Must preserve (3):** `apples`, `eggs`, `milk`
- **Raw STT:**
  ```
  first apples second eggs third milk
  ```
- **Preprocessed** (10ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  First apples second eggs third milk.
  ```

#### `casual` ✅ — preservation 3/3 (100%), LLM 6466ms, total 6476ms

> _hint: inline: apples, eggs, and milk_

```
First apples, second eggs, and third milk.
```

#### `normal` ✅ — preservation 3/3 (100%), LLM 8430ms, total 8436ms

> _hint: bulleted list_

```
- apples
- eggs
- milk
```

#### `formal` ✅ — preservation 3/3 (100%), LLM 2845ms, total 2852ms

> _hint: numbered list_

```
The grocery list includes the following items:

1. Apples
2. Eggs
3. Milk
```

---

### `02_enum_medium` — verbal_enumerated_list (medium)

- **Intent:** Four-item keyboard supply list with a verbal preamble.
- **Must preserve (5):** `keyboard supplies`, `air duster`, `alcohol wipes`, `extra cable`, `microfiber cloth`
- **Raw STT:**
  ```
  here i have a list of keyboard supplies first thing is air duster second is alcohol wipes third is an extra cable and fourth is a microfiber cloth
  ```
- **Preprocessed** (8ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  Here i have a list of keyboard supplies first thing is air duster second is alcohol wipes third is an extra cable and fourth is a microfiber cloth.
  ```

#### `casual` ✅ — preservation 5/5 (100%), LLM 534ms, total 543ms

> _hint: inline with lead-in_

```
Here's my list of keyboard supplies: air duster, alcohol wipes, an extra cable, and a microfiber cloth.
```

#### `normal` ✅ — preservation 5/5 (100%), LLM 1127ms, total 1134ms

> _hint: lead-in + bullets_

```
Here's my list of keyboard supplies:

- air duster
- alcohol wipes
- extra cable
- microfiber cloth
```

#### `formal` ✅ — preservation 5/5 (100%), LLM 1793ms, total 1801ms

> _hint: lead-in + numbered list with light register lift_

```
I am compiling a list of keyboard supplies. The items are as follows:

1. Air duster
2. Alcohol wipes
3. An extra cable
4. A microfiber cloth
```

---

### `03_enum_long` — verbal_enumerated_list (long)

- **Intent:** Five-item meeting agenda with a verbal lead-in and contextual preamble.
- **Must preserve (6):** `weekly review meeting`, `cleanup pipeline`, `learning loop`, `outstanding bugs`, `design language phase six`, `open floor`
- **Raw STT:**
  ```
  okay for the weekly review meeting agenda i want to cover the following items first the cleanup pipeline status update including the prompt iteration work that bernard is doing second the learning loop nightly run results from this past week third any outstanding bugs from the qa kitten smoke runs fourth the design language phase six cutover retrospective and fifth open floor for new topics
  ```
- **Preprocessed** (7ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  Okay for the weekly review meeting agenda i want to cover the following items first the cleanup pipeline status update including the prompt iteration work that bernard is doing second the learning loop nightly run results from this past week third any outstanding bugs from the qa kitten smoke runs fourth the design language phase six cutover retrospective and fifth open floor for new topics.
  ```

#### `casual` ✅ — preservation 6/6 (100%), LLM 1386ms, total 1394ms

> _hint: probably awkward — long lists don't read inline_

```
Okay, for the weekly review meeting agenda, I want to cover the following items: first, the cleanup pipeline status update including the prompt iteration work that Bernard is doing; second, the learning loop nightly run results from this past week; third, any outstanding bugs from the QA kitten smoke runs; fourth, the design language phase six cutover retrospective; fifth, open floor for new topics.
```

#### `normal` ✅ — preservation 6/6 (100%), LLM 2920ms, total 2927ms

> _hint: bulleted with lead-in_

```
For the weekly review meeting agenda, I want to cover the following items:

- The cleanup pipeline status update, including the prompt iteration work that Bernard is doing
- The learning loop nightly run results from this past week
- Any outstanding bugs from the QA kitten smoke runs
- The design language phase six cutover retrospective
- Open floor for new topics
```

#### `formal` ✅ — preservation 6/6 (100%), LLM 3683ms, total 3691ms

> _hint: header + numbered list_

```
For the weekly review meeting agenda, I want to cover the following items:

1. The status update on the cleanup pipeline, including the prompt iteration work that Bernard is doing.
2. The learning loop nightly run results from this past week.
3. Any outstanding bugs from the QA kitten smoke runs.
4. The design language phase six cutover retrospective.
5. Open floor for new topics.
```

---

### `04_implicit_short` — implicit_list (short)

- **Intent:** Comma-series shopping list, no verbal cue.
- **Must preserve (4):** `milk`, `eggs`, `bread`, `butter`
- **Raw STT:**
  ```
  i need milk eggs bread butter
  ```
- **Preprocessed** (7ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  I need milk eggs bread butter.
  ```

#### `casual` ✅ — preservation 4/4 (100%), LLM 306ms, total 314ms

> _hint: inline prose: I need milk, eggs, bread, and butter._

```
I need milk, eggs, bread, and butter.
```

#### `normal` ✅ — preservation 4/4 (100%), LLM 606ms, total 613ms

> _hint: JUDGMENT CALL — could be inline or bullets; ambiguous_

```
I need milk, eggs, bread, and butter.
```

#### `formal` ✅ — preservation 4/4 (100%), LLM 936ms, total 944ms

> _hint: probably bulleted_

```
Could you please pick up milk, eggs, bread, and butter on your way home?
```

---

### `05_implicit_medium` — implicit_list (medium)

- **Intent:** Tech-dependency mention as part of a planning thought.
- **Must preserve (6):** `tauri`, `rusqlite`, `whisper`, `cpal`, `ort`, `phase seven`
- **Raw STT:**
  ```
  the project depends on tauri rusqlite whisper rs cpal ort and a couple of other crates we should audit them all before phase seven
  ```
- **Preprocessed** (8ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  The project depends on tauri rusqlite whisper rs cpal ort and a couple of other crates we should audit them all before phase seven.
  ```

#### `casual` ✅ — preservation 6/6 (100%), LLM 715ms, total 723ms

> _hint: inline prose with conjunction_

```
The project depends on tauri, rusqlite, whisper, rs, cpal, ort, and a couple of other crates. We should audit them all before phase seven.
```

#### `normal` ✅ — preservation 6/6 (100%), LLM 1566ms, total 1572ms

> _hint: JUDGMENT — could go either way_

```
The project depends on tauri, rusqlite, whisper, rs, cpal, ort, and a couple of other crates. We should audit them all before phase seven.
```

#### `formal` ✅ — preservation 6/6 (100%), LLM 1780ms, total 1787ms

> _hint: probably bulleted dependency list_

```
The project depends on Tauri, Rusqlite, Whisper, Rs, Cpal, Ort, and a couple of other crates. We should audit them all before Phase Seven.
```

---

### `06_implicit_long` — implicit_list (long)

- **Intent:** Stream-of-consciousness enumeration of open work, fairly long.
- **Must preserve (7):** `audio streaming`, `llm skip`, `design language sealing`, `ai command modes`, `phase five six seven`, `mode tuning`, `smaller cleanup tasks`
- **Raw STT:**
  ```
  looking at the open issues we have audio streaming which is the standing p one then there is the llm skip for short casual utterances the design language sealing which is now done the hidden ai command modes work and the phase five six seven main phase work which is the long pole and also the mode tuning empirical iteration which is what we are doing right now and probably some smaller cleanup tasks that have not been triaged
  ```
- **Preprocessed** (7ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  Looking at the open issues we have audio streaming which is the standing p one then there is the llm skip for short casual utterances the design language sealing which is now done the hidden ai command modes work and the phase five six seven main phase work which is the long pole and also the mode tuning empirical iteration which is what we are doing right now and probably some smaller cleanup tasks that have not been triaged.
  ```

#### `casual` ✅ — preservation 7/7 (100%), LLM 1420ms, total 1428ms

> _hint: inline is going to be a wall — probably acceptable_

```
Looking at the open issues, we have audio streaming which is the standing P1, then there's the LLM skip for short casual utterances. Design language sealing is done, hidden AI command modes work, and phase five six seven main phase work—long pole—and also mode tuning empirical iteration, which is what we're doing right now. Probably some smaller cleanup tasks that haven't been triaged.
```

#### `normal` ✅ — preservation 7/7 (100%), LLM 3681ms, total 3688ms

> _hint: bulleted with prose intro_

```
Looking at the open issues, we have audio streaming, which is the standing priority one. Then there is the LLM skip for short casual utterances. The design language sealing is now done. The hidden AI command modes work, and the phase five, six, seven main phase work, which is the long pole. Also, the mode tuning empirical iteration is what we are doing right now, and probably some smaller cleanup tasks that have not been triaged.
```

#### `formal` ⚠️ — preservation 6/7 (86%), LLM 4358ms, total 4366ms

> _hint: header + bulleted list of items, prose framing_

```
Looking at the open issues, we have audio streaming, which is the standing priority one. Then there is the LLM skip for short casual utterances, the design language sealing, which is now done, the hidden AI command modes work, and the phase five, six, and seven main phase work, which is the long pole. Additionally, there is the mode tuning empirical iteration, which is what we are doing right now, and probably some smaller cleanup tasks that have not been triaged.
```

_missed must-preserve:_ `phase five six seven`

---

### `07_runon_short` — run_on_sentence (short)

- **Intent:** Three loosely-joined assertions in one breath.
- **Must preserve (5):** `ship`, `today`, `ready`, `tests pass`, `qa kitten`
- **Raw STT:**
  ```
  we should ship this today and i think it is ready and the tests pass and qa kitten signed off
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  We should ship this today and i think it is ready and the tests pass and qa kitten signed off.
  ```

#### `casual` ✅ — preservation 5/5 (100%), LLM 501ms, total 507ms

> _hint: one sentence with commas is fine_

```
We should ship this today and I think it's ready. And the tests pass, QA Kitten signed off.
```

#### `normal` ✅ — preservation 5/5 (100%), LLM 1088ms, total 1096ms

> _hint: split into 2-3 sentences_

```
We should ship this today, and I think it is ready. The tests pass, and QA Kitten signed off.
```

#### `formal` ⚠️ — preservation 4/5 (80%), LLM 1322ms, total 1330ms

> _hint: split + tighten register_

```
We should ship this today, and I believe it is ready. The tests have passed, and QA Kitten has signed off.
```

_missed must-preserve:_ `tests pass`

---

### `08_runon_medium` — run_on_sentence (medium)

- **Intent:** Single run-on describing the cleanup pipeline architecture.
- **Must preserve (8):** `raw whisper output`, `deterministic preprocessor`, `fillers`, `verbal cues`, `local llm`, `style work`, `injected`, `focused`
- **Raw STT:**
  ```
  the cleanup pipeline takes the raw whisper output runs it through the deterministic preprocessor which handles fillers and verbal cues then sends the result to the local llm which does the actual style work and the output gets injected into whatever app the user has focused at the moment of release
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  The cleanup pipeline takes the raw whisper output runs it through the deterministic preprocessor which handles fillers and verbal cues then sends the result to the local llm which does the actual style work and the output gets injected into whatever app the user has focused at the moment of release.
  ```

#### `casual` ✅ — preservation 8/8 (100%), LLM 1053ms, total 1060ms

> _hint: 2-3 sentences_

```
The cleanup pipeline takes the raw whisper output, runs it through the deterministic preprocessor which handles fillers and verbal cues, then sends the result to the local LLM which does the actual style work. The output gets injected into whatever app the user has focused at the moment of release.
```

#### `normal` ✅ — preservation 8/8 (100%), LLM 2416ms, total 2423ms

> _hint: 3-4 sentences with paragraph structure_

```
The cleanup pipeline takes the raw whisper output, runs it through the deterministic preprocessor which handles fillers and verbal cues, then sends the result to the local llm which does the actual style work. The output gets injected into whatever app the user has focused on at the moment of release.
```

#### `formal` ⚠️ — preservation 6/8 (75%), LLM 2965ms, total 2975ms

> _hint: multiple sentences, possibly a process diagram-ish list_

```
The cleanup pipeline processes raw Whisper output by running it through a deterministic preprocessor that manages fillers and verbal cues. The resulting text is then sent to the local language model, which performs the actual stylistic work. The final output is injected into whatever application the user has focused on at the moment of release.
```

_missed must-preserve:_ `local llm`, `style work`

---

### `09_runon_long` — run_on_sentence (long)

- **Intent:** Long retrospective monologue about project evolution.
- **Must preserve (12):** `wisprflow`, `local and private`, `deterministic preprocessor`, `llm polish`, `learning loop`, `design system`, `six waves`, `judges`, `hooks`, `project agents`, `mode tuning`, `release`
- **Raw STT:**
  ```
  when we started this project the goal was just to replace wisprflow with something local and private but over time we have built up this whole thing with a deterministic preprocessor that handles all the rule shaped work and an llm polish layer that does the judgment work and a learning loop that runs nightly to improve based on user corrections and a design system that took six waves to get right and an entire phase architecture with judges and hooks and project agents and now we are in the empirical mode tuning phase which is hopefully the last big quality push before we ship a real release
  ```
- **Preprocessed** (8ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  When we started this project the goal was just to replace wisprflow with something local and private but over time we have built up this whole thing with a deterministic preprocessor that handles all the rule shaped work and an llm polish layer that does the judgment work and a learning loop that runs nightly to improve based on user corrections and a design system that took six waves to get right and an entire phase architecture with judges and hooks and project agents and now we are in the empirical mode tuning phase which is hopefully the last big quality push before we ship a real release.
  ```

#### `casual` ✅ — preservation 12/12 (100%), LLM 1914ms, total 1923ms

> _hint: probably 4-6 sentences with breaks_

```
When we started this project, the goal was just to replace wisprflow with something local and private. But over time, we built up this whole thing with a deterministic preprocessor that handles all the rule-shaped work and an LLM polish layer that does the judgment work. We also have a learning loop that runs nightly to improve based on user corrections. There's a design system that took six waves to get right, and an entire phase architecture with judges, hooks, project agents. Now we're in the empirical mode, tuning for the real release.
```

#### `normal` ✅ — preservation 12/12 (100%), LLM 5046ms, total 5053ms

> _hint: split into 2-3 paragraphs_

```
When we started this project, the goal was just to replace wisprflow with something local and private. But over time, we have built up this whole thing with a deterministic preprocessor that handles all the rule-shaped work, and an LLM polish layer that does the judgment work. There is also a learning loop that runs nightly to improve based on user corrections. We have developed a design system that took six waves to get right, and an entire phase architecture with judges, hooks, and project agents. Now we are in the empirical mode tuning phase, which is hopefully the last big quality push before we ship a real release.
```

#### `formal` ✅ — preservation 12/12 (100%), LLM 5359ms, total 5369ms

> _hint: structured retrospective with possible section header_

```
When we started this project, the goal was simply to replace WisprFlow with something local and private. Over time, however, we have built up an entire system that includes a deterministic preprocessor handling all rule-shaped work, an LLM polish layer for judgment tasks, a learning loop running nightly to improve based on user corrections, a design system that took six waves to perfect, and an entire phase architecture with judges, hooks, and project agents. Now we are in the empirical mode tuning phase, which is hopefully the last big quality push before we ship a real release.
```

---

### `10_stream_short` — stream_of_consciousness (short)

- **Intent:** Hesitant musing about pipeline reordering; fillers present.
- **Must preserve (2):** `cleanup pipeline`, `reorder`
- **Raw STT:**
  ```
  so the thing is um i was thinking about the the cleanup pipeline and like maybe we should reorder it
  ```
- **Preprocessed** (7ms, fillers=1, stutters=1, self-corr=0, cues=0): 
  ```
  So the thing is i was thinking about the cleanup pipeline and like maybe we should reorder it.
  ```

#### `casual` ✅ — preservation 2/2 (100%), LLM 455ms, total 463ms

> _hint: drop fillers but keep the musing tone_

```
So the thing is, I was thinking about the cleanup pipeline and like maybe we should reorder it.
```

#### `normal` ✅ — preservation 2/2 (100%), LLM 982ms, total 989ms

> _hint: drop fillers, polish_

```
So, the thing is, I was thinking about the cleanup pipeline and like maybe we should reorder it.
```

#### `formal` ✅ — preservation 2/2 (100%), LLM 550ms, total 560ms

> _hint: drop fillers + lift register_

```
The cleanup pipeline should perhaps be reordered.
```

---

### `11_stream_medium` — stream_of_consciousness (medium)

- **Intent:** Internal deliberation about preprocessor vs LLM division of labor.
- **Must preserve (5):** `preprocessor`, `more aggressive`, `llm`, `consistent`, `new prompts`
- **Raw STT:**
  ```
  okay so like the question is whether we want to make the preprocessor more aggressive or whether we should just let the llm handle more of it i guess it depends on how much we trust the llm to be consistent which is honestly not super high right now but maybe with the new prompts it will be better
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  Okay so like the question is whether we want to make the preprocessor more aggressive or whether we should just let the llm handle more of it i guess it depends on how much we trust the llm to be consistent which is honestly not super high right now but maybe with the new prompts it will be better.
  ```

#### `casual` ✅ — preservation 5/5 (100%), LLM 1170ms, total 1177ms

> _hint: preserve the deliberative feel_

```
Okay so like the question is whether we want to make the preprocessor more aggressive or whether we should just let the LLM handle more of it. I guess it depends on how much we trust the LLM to be consistent, which is honestly not super high right now but maybe with the new prompts it will be better.
```

#### `normal` ✅ — preservation 5/5 (100%), LLM 2792ms, total 2799ms

> _hint: split into clean sentences but keep the open question_

```
Okay, so like the question is whether we want to make the preprocessor more aggressive or whether we should just let the LLM handle more of it. I guess it depends on how much we trust the LLM to be consistent, which is honestly not super high right now, but maybe with the new prompts it will be better.
```

#### `formal` ⚠️ — preservation 4/5 (80%), LLM 3160ms, total 3167ms

> _hint: restructure as a 'two options' framing_

```
The question arises as to whether we should make the preprocessor more aggressive or allow the language model (LLM) to handle a greater portion of the preprocessing. This likely depends on our level of trust in the LLM's consistency, which currently is not particularly high. However, with the introduction of new prompts, this may improve.
```

_missed must-preserve:_ `consistent`

---

### `12_stream_long` — stream_of_consciousness (long)

- **Intent:** Long meandering thought about mode design, register vs target, and latency.
- **Must preserve (12):** `raw audio`, `usable`, `slack message`, `meeting note`, `formal document`, `three modes`, `right axis`, `output target`, `register`, `latency`, `two seconds`, `stop using`
- **Raw STT:**
  ```
  i was just thinking about how dictation is interesting because you have this raw audio that gets turned into text and then the text needs to become something usable and there is this whole question of what does usable mean for different contexts like a slack message versus a meeting note versus a formal document and we have these three modes but i wonder if that is even the right axis maybe it should be more about output target than about register and also there is the whole thing about latency where if you take more than two seconds people just stop using the tool entirely which is a hard constraint
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  I was just thinking about how dictation is interesting because you have this raw audio that gets turned into text and then the text needs to become something usable and there is this whole question of what does usable mean for different contexts like a slack message versus a meeting note versus a formal document and we have these three modes but i wonder if that is even the right axis maybe it should be more about output target than about register and also there is the whole thing about latency where if you take more than two seconds people just stop using the tool entirely which is a hard constraint.
  ```

#### `casual` ✅ — preservation 12/12 (100%), LLM 2062ms, total 2068ms

> _hint: preserve the meandering quality, drop fillers_

```
I was just thinking about how dictation is interesting because you have this raw audio that gets turned into text, and then the text needs to become something usable. And there's this whole question of what "usable" means for different contexts—like a Slack message versus a meeting note versus a formal document—and we have these three modes, but I wonder if that is even the right axis. Maybe it should be more about output target than just register. Also, there's this whole thing about latency where if you take more than two seconds people just stop using the tool entirely—it's a hard constraint.
```

#### `normal` ✅ — preservation 12/12 (100%), LLM 4911ms, total 4918ms

> _hint: split into thought-paragraphs_

```
I was just thinking about how dictation is interesting because you have this raw audio that gets turned into text, and then the text needs to become something usable. There is this whole question of what does "usable" mean for different contexts—like a Slack message versus a meeting note versus a formal document. We have these three modes, but I wonder if that is even the right axis. Maybe it should be more about output target than about register. Also, there is the whole thing about latency where if you take more than two seconds, people just stop using the tool entirely, which is a hard constraint.
```

#### `formal` ✅ — preservation 12/12 (100%), LLM 5498ms, total 5505ms

> _hint: restructure aggressively with sections — risk of losing meandering quality_

```
I was just thinking about how dictation is interesting because you have this raw audio that gets turned into text, and then the text needs to become something usable. There is this whole question of what does "usable" mean for different contexts—like a Slack message versus a meeting note versus a formal document. We have these three modes, but I wonder if that is even the right axis. Maybe it should be more about the output target than about register. Additionally, there is the whole issue of latency; if it takes more than two seconds, people just stop using the tool entirely, which is a hard constraint.
```

---

### `13_chrono_short` — chronological_narrative (short)

- **Intent:** Three-event morning sequence.
- **Must preserve (3):** `woke up`, `coffee`, `walk`
- **Raw STT:**
  ```
  i woke up made coffee then went for a walk
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  I woke up made coffee then went for a walk.
  ```

#### `casual` ✅ — preservation 3/3 (100%), LLM 338ms, total 344ms

> _hint: natural narrative_

```
I woke up, made coffee, then went for a walk.
```

#### `normal` ✅ — preservation 3/3 (100%), LLM 717ms, total 724ms

> _hint: natural narrative with commas/period_

```
I woke up, made coffee, and then went for a walk.
```

#### `formal` ✅ — preservation 3/3 (100%), LLM 780ms, total 787ms

> _hint: narrative or list — judgment_

```
I woke up, made coffee, and then went for a walk.
```

---

### `14_chrono_medium` — chronological_narrative (medium)

- **Intent:** Five-step dev workflow sequence with explicit 'then' markers.
- **Must preserve (7):** `pulled`, `main`, `migrations`, `tauri dev server`, `recording overlay`, `committed`, `pushed`
- **Raw STT:**
  ```
  first i pulled the latest from main then i ran the migrations then i started the tauri dev server then i tested the recording overlay then i committed and pushed
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  First i pulled the latest from main then i ran the migrations then i started the tauri dev server then i tested the recording overlay then i committed and pushed.
  ```

#### `casual` ✅ — preservation 7/7 (100%), LLM 732ms, total 739ms

> _hint: narrative with commas_

```
I pulled the latest from main, then I ran the migrations, then I started the tauri dev server. Then I tested the recording overlay, and finally, I committed and pushed.
```

#### `normal` ✅ — preservation 7/7 (100%), LLM 1833ms, total 1841ms

> _hint: could be numbered list given the explicit steps_

```
First, I pulled the latest from main. Then, I ran the migrations. Next, I started the Tauri dev server. After that, I tested the recording overlay. Finally, I committed and pushed.
```

#### `formal` ⚠️ — preservation 6/7 (86%), LLM 1728ms, total 1736ms

> _hint: numbered list with header like 'Workflow:'_

```
I pulled the latest changes from the main branch, ran the migrations, started the Tauri development server, tested the recording overlay, and then committed and pushed the changes.
```

_missed must-preserve:_ `tauri dev server`

---

### `15_chrono_long` — chronological_narrative (long)

- **Intent:** Detailed chronological recap of a planning + execution session.
- **Must preserve (12):** `planning session`, `bd queue`, `mode tuning`, `adr drafts`, `existing prompts`, `four waves`, `fixture set`, `eval harness`, `baseline`, `iteration two`, `iteration three`, `migration ten`
- **Raw STT:**
  ```
  this morning started with a planning session where dustin reviewed the bd queue and prioritized the mode tuning work then bernard read the relevant adr drafts and the existing prompts then we set up the bd issue chain for the four waves then bernard wrote the fixture set and the eval harness then we ran the baseline and tabulated results then we authored the new prompts then we ran iteration two then iteration three then we shipped migration ten and updated status
  ```
- **Preprocessed** (7ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  This morning started with a planning session where dustin reviewed the bd queue and prioritized the mode tuning work then bernard read the relevant adr drafts and the existing prompts then we set up the bd issue chain for the four waves then bernard wrote the fixture set and the eval harness then we ran the baseline and tabulated results then we authored the new prompts then we ran iteration two then iteration three then we shipped migration ten and updated status.
  ```

#### `casual` ✅ — preservation 12/12 (100%), LLM 1609ms, total 1616ms

> _hint: long narrative — fine but trim fillers_

```
This morning started with a planning session where Dustin reviewed the BD queue and prioritized the mode tuning work, then Bernard read the relevant ADR drafts and the existing prompts. We set up the BD issue chain for the four waves next. Bernard wrote the fixture set and the eval harness. We ran the baseline and tabulated results. Then we authored the new prompts. We ran iteration two, then iteration three. Finally, we shipped migration ten and updated status.
```

#### `normal` ✅ — preservation 12/12 (100%), LLM 3853ms, total 3861ms

> _hint: narrative split into 2-3 paragraphs OR numbered list_

```
This morning started with a planning session where Dustin reviewed the BD queue and prioritized the mode tuning work. Then Bernard read the relevant ADR drafts and the existing prompts. Then we set up the BD issue chain for the four waves. Then Bernard wrote the fixture set and the eval harness. Then we ran the baseline and tabulated results. Then we authored the new prompts. Then we ran iteration two, then iteration three. Then we shipped migration ten and updated status.
```

#### `formal` ⚠️ — preservation 9/12 (75%), LLM 4099ms, total 4108ms

> _hint: numbered process list with header is probably right_

```
This morning began with a planning session where Dustin reviewed the BD queue and prioritized the mode tuning work. Bernard then read the relevant ADR drafts and existing prompts. We subsequently set up the BD issue chain for the four waves. Bernard wrote the fixture set and the evaluation harness. We ran the baseline, tabulated the results, authored the new prompts, and conducted iterations two and three. Finally, we shipped migration ten and updated the status.
```

_missed must-preserve:_ `eval harness`, `iteration two`, `iteration three`

---

### `16_qa_short` — qa_monologue (short)

- **Intent:** Self-Q&A: should we ship? Tests pass, so yes.
- **Must preserve (3):** `ship`, `tests pass`, `yes`
- **Raw STT:**
  ```
  should we ship it well the tests pass so yes
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  Should we ship it well the tests pass so yes.
  ```

#### `casual` ✅ — preservation 3/3 (100%), LLM 371ms, total 378ms

> _hint: natural_

```
Should we ship it? Well, the tests pass. So, yes.
```

#### `normal` ✅ — preservation 3/3 (100%), LLM 746ms, total 754ms

> _hint: natural with proper punctuation_

```
Should we ship it? Well, the tests pass. So, yes.
```

#### `formal` ✅ — preservation 3/3 (100%), LLM 720ms, total 728ms

> _hint: rephrase as statement rather than self-dialog_

```
We should ship it since the tests pass, so yes.
```

---

### `17_qa_medium` — qa_monologue (medium)

- **Intent:** Self-Q&A about model selection by hardware capability.
- **Must preserve (7):** `normal mode`, `six gig vram`, `seven b quantized`, `good quality`, `three b`, `faster`, `less reliable`
- **Raw STT:**
  ```
  what is the right model for normal mode well it depends on the user hardware on a six gig vram card we can run the seven b quantized and that gives us good quality but on something smaller we have to fall back to three b which is faster but less reliable
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  What is the right model for normal mode well it depends on the user hardware on a six gig vram card we can run the seven b quantized and that gives us good quality but on something smaller we have to fall back to three b which is faster but less reliable.
  ```

#### `casual` ⚠️ — preservation 5/7 (71%), LLM 1044ms, total 1051ms

> _hint: natural self-deliberation_

```
The right model for normal mode depends on the user's hardware. On a six gig VRAM card, we can run the 7B quantized model for good quality, but for something smaller, we have to rely on the 3B model which is faster but less reliable.
```

_missed must-preserve:_ `seven b quantized`, `three b`

#### `normal` ✅ — preservation 7/7 (100%), LLM 2674ms, total 2682ms

> _hint: Q at top, answer flows_

```
What is the right model for normal mode? Well, it depends on the user's hardware. On a six gig VRAM card, we can run the seven B quantized, and that gives us good quality. But on something smaller, we have to fall back to three B, which is faster but less reliable.
```

#### `formal` ⚠️ — preservation 4/7 (57%), LLM 2825ms, total 2833ms

> _hint: could become 'Model selection criteria:' with structured answer_

```
The appropriate model for normal mode depends on the user's hardware. On a six gigabytes VRAM card, we can run the 7B quantized version, which provides good quality. However, on smaller devices, we must fallback to the 3B model, which is faster but less reliable.
```

_missed must-preserve:_ `six gig vram`, `seven b quantized`, `three b`

---

### `18_qa_long` — qa_monologue (long)

- **Intent:** Detailed Q&A about whether to add a code mode, weighing three considerations.
- **Must preserve (12):** `code mode`, `programmers`, `variable names`, `snippets`, `three modes axis`, `register`, `orthogonal`, `implementation cost`, `few shot examples`, `coder model`, `not add it yet`, `after the empirical tuning`
- **Raw STT:**
  ```
  the question is whether to add a code mode and the answer is complicated because on one hand programmers dictate variable names and snippets all the time and a code mode would be valuable on the other hand the current three modes axis is register based and code is orthogonal to register so adding code would muddy the model and the third consideration is implementation cost which would mean a new prompt new few shot examples possibly a different model since code likely benefits from a coder model so probably we should not add it yet but revisit after the empirical tuning is done and we have a clearer sense of where the modes are weakest
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  The question is whether to add a code mode and the answer is complicated because on one hand programmers dictate variable names and snippets all the time and a code mode would be valuable on the other hand the current three modes axis is register based and code is orthogonal to register so adding code would muddy the model and the third consideration is implementation cost which would mean a new prompt new few shot examples possibly a different model since code likely benefits from a coder model so probably we should not add it yet but revisit after the empirical tuning is done and we have a clearer sense of where the modes are weakest.
  ```

#### `casual` ⚠️ — preservation 10/12 (83%), LLM 2128ms, total 2134ms

> _hint: preserve the back-and-forth_

```
The question is whether to add a code mode, and the answer is complicated. On one hand, programmers dictate variable names and snippets all the time, and a code mode would be valuable. On the other hand, the current three modes axis is register-based, and code is orthogonal to register. Adding code would muddy the model, and there are implementation costs involved—new prompts, few-shot examples, possibly a different model since code likely benefits from a coder-specific model. So, we should not add it yet; let's revisit after empirical tuning is done and we have a clearer sense of where the modes are weakest.
```

_missed must-preserve:_ `coder model`, `after the empirical tuning`

#### `normal` ✅ — preservation 12/12 (100%), LLM 5270ms, total 5279ms

> _hint: Q + answer paragraphs_

```
The question is whether to add a code mode, and the answer is complicated. On one hand, programmers dictate variable names and snippets all the time, and a code mode would be valuable. On the other hand, the current three modes axis is register-based, and code is orthogonal to register, so adding code would muddy the model. The third consideration is implementation cost, which would mean a new prompt, new few-shot examples, possibly a different model since code likely benefits from a coder model. So, probably we should not add it yet but revisit after the empirical tuning is done and we have a clearer sense of where the modes are weakest.
```

#### `formal` ⚠️ — preservation 8/12 (67%), LLM 6028ms, total 6036ms

> _hint: structured: 'Question:', 'Considerations:', 'Recommendation:'_

```
The question regarding the addition of a code mode is complex. On one hand, programmers frequently dictate variable names and snippets, making a code mode valuable. However, the current three-mode axis is register-based, with code being orthogonal to this register system. Introducing a code mode could potentially muddy the model. Additionally, there are implementation cost considerations, which might necessitate a new prompt, few-shot examples, or even a different model specifically for coding tasks since code likely benefits from a coder-oriented model. Therefore, we should probably not add it at this time but revisit the decision after empirical tuning is complete and we have a clearer understanding of where the existing modes are weakest.
```

_missed must-preserve:_ `three modes axis`, `coder model`, `not add it yet`, `after the empirical tuning`

---

### `19_tech_short` — technical_content (short)

- **Intent:** Technical description of a Rust function signature.
- **Must preserve (3):** `arc mutex connection`, `app result`, `string`
- **Raw STT:**
  ```
  the function takes an arc mutex connection and returns app result string
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  The function takes an arc mutex connection and returns app result string.
  ```

#### `casual` ✅ — preservation 3/3 (100%), LLM 367ms, total 374ms

> _hint: lowercase tech terms OK_

```
The function takes an arc mutex connection and returns an app result string.
```

#### `normal` ✅ — preservation 3/3 (100%), LLM 729ms, total 735ms

> _hint: Arc<Mutex<Connection>> would be ideal but probably won't happen without dict_

```
The function takes an arc mutex connection and returns an app result string.
```

#### `formal` ✅ — preservation 3/3 (100%), LLM 808ms, total 816ms

> _hint: tight technical prose_

```
The function takes an arc mutex connection and returns an app result string.
```

---

### `20_tech_medium` — technical_content (medium)

- **Intent:** Description of a Rust struct's fields and constructor.
- **Must preserve (7):** `llm cleaner`, `box dyn cleanup provider`, `arc mutex connection`, `model id`, `temperature`, `max tokens`, `constructor`
- **Raw STT:**
  ```
  the llm cleaner struct holds a box dyn cleanup provider an arc mutex connection a model id string a temperature f32 and a max tokens u32 the constructor takes these as arguments and stores them
  ```
- **Preprocessed** (7ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  The llm cleaner struct holds a box dyn cleanup provider an arc mutex connection a model id string a temperature f32 and a max tokens u32 the constructor takes these as arguments and stores them.
  ```

#### `casual` ✅ — preservation 7/7 (100%), LLM 919ms, total 926ms

> _hint: lowercase tech is fine_

```
The LLM cleaner struct holds a box, dyn cleanup provider, an arc mutex connection, a model ID string, a temperature (f32), and a max tokens (u32). The constructor takes these as arguments and stores them.
```

#### `normal` ⚠️ — preservation 2/7 (29%), LLM 2735ms, total 2743ms

> _hint: should attempt type formatting_

```
The `llm_cleaner` struct holds a box `dyn_cleanup_provider`, an `arc_mutex`, a `connection`, a `model_id` string, a `temperature` of type `f32`, and a `max_tokens` of type `u32`. The constructor takes these as arguments and stores them.
```

_missed must-preserve:_ `llm cleaner`, `box dyn cleanup provider`, `arc mutex connection`, `model id`, `max tokens`

#### `formal` ⚠️ — preservation 3/7 (43%), LLM 2888ms, total 2896ms

> _hint: code-block formatting for types ideally_

```
The `LlmCleanerStruct` holds a box to a dynamic cleanup provider, an arc mutex for connection, a model ID as a string, a temperature as an `f32`, and a maximum number of tokens as a `u32`. The constructor takes these as arguments and stores them.
```

_missed must-preserve:_ `llm cleaner`, `box dyn cleanup provider`, `arc mutex connection`, `max tokens`

---

### `21_tech_long` — technical_content (long)

- **Intent:** Architecture explanation: shared DB connection via Arc<Mutex<Connection>>.
- **Must preserve (11):** `dictation thread`, `arc mutex connection`, `orchestrator`, `cleaner`, `prompt`, `dictionary`, `few shot examples`, `persist transaction`, `rusqlite`, `single threaded`, `mutex serializes`
- **Raw STT:**
  ```
  the architecture is that the dictation thread owns an arc mutex connection and shares a clone with the orchestrator which in turn shares another clone with the cleaner so when cleanup runs it can issue its own queries for the prompt the dictionary and the few shot examples without negotiating borrows across the orchestrators persist transaction and this works because rusqlite connections are themselves single threaded but the mutex serializes access across the threads that hold clones
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  The architecture is that the dictation thread owns an arc mutex connection and shares a clone with the orchestrator which in turn shares another clone with the cleaner so when cleanup runs it can issue its own queries for the prompt the dictionary and the few shot examples without negotiating borrows across the orchestrators persist transaction and this works because rusqlite connections are themselves single threaded but the mutex serializes access across the threads that hold clones.
  ```

#### `casual` ✅ — preservation 11/11 (100%), LLM 1597ms, total 1603ms

> _hint: tech narrative is fine_

```
The architecture is that the dictation thread owns an arc mutex connection and shares a clone with the orchestrator, which in turn shares another clone with the cleaner. So when cleanup runs, it can issue its own queries for the prompt, dictionary, and few shot examples without negotiating borrows across the orchestrators' persist transaction. This works because rusqlite connections are themselves single-threaded, but the mutex serializes access across the threads that hold clones.
```

#### `normal` ⚠️ — preservation 10/11 (91%), LLM 3856ms, total 3864ms

> _hint: 2-3 sentence paragraphs_

```
The architecture is that the dictation thread owns an arc mutex connection and shares a clone with the orchestrator, which in turn shares another clone with the cleaner. So when cleanup runs, it can issue its own queries for the prompt, the dictionary, and the few-shot examples without negotiating borrows across the orchestrator's persistent transaction. And this works because rusqlite connections are themselves single-threaded but the mutex serializes access across the threads that hold clones.
```

_missed must-preserve:_ `persist transaction`

#### `formal` ⚠️ — preservation 10/11 (91%), LLM 4230ms, total 4238ms

> _hint: could become 'Architecture overview' with structured explanation_

```
The architecture is as follows: The dictation thread owns an arc-mutex connection and shares a clone with the orchestrator, which in turn shares another clone with the cleaner. When cleanup runs, it can issue its own queries for the prompt, dictionary, and few-shot examples without negotiating borrows across the orchestrator's persistent transaction. This works because rusqlite connections are themselves single-threaded, but the mutex serializes access across the threads that hold clones.
```

_missed must-preserve:_ `persist transaction`

---

### `22_casual_short` — casual_chat (short)

- **Intent:** Quick text-message-style request.
- **Must preserve (2):** `milk`, `way home`
- **Raw STT:**
  ```
  hey can you grab milk on the way home
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  Hey can you grab milk on the way home.
  ```

#### `casual` ✅ — preservation 2/2 (100%), LLM 311ms, total 317ms

> _hint: punctuated text message_

```
Hey, can you grab milk on the way home?
```

#### `normal` ✅ — preservation 2/2 (100%), LLM 526ms, total 534ms

> _hint: punctuated, slightly tightened_

```
Can you grab milk on the way home?
```

#### `formal` ✅ — preservation 2/2 (100%), LLM 665ms, total 672ms

> _hint: INAPPROPRIATE mode for this content but should still preserve_

```
Could you please pick up milk on your way home?
```

---

### `23_casual_medium` — casual_chat (medium)

- **Intent:** Casual movie recommendation message with enthusiasm.
- **Must preserve (6):** `movie`, `really good`, `better than i expected`, `cinematography`, `soundtrack`, `go see it again`
- **Raw STT:**
  ```
  yo so the movie was actually really good way better than i expected the cinematography was incredible and the soundtrack was on point we should definitely go see it again sometime
  ```
- **Preprocessed** (5ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  Yo so the movie was actually really good way better than i expected the cinematography was incredible and the soundtrack was on point we should definitely go see it again sometime.
  ```

#### `casual` ✅ — preservation 6/6 (100%), LLM 737ms, total 743ms

> _hint: preserve 'yo' and casual register_

```
Yo, so the movie was actually really good, way better than I expected. The cinematography was incredible, and the soundtrack was on point. We should definitely go see it again sometime.
```

#### `normal` ✅ — preservation 6/6 (100%), LLM 1662ms, total 1670ms

> _hint: drop 'yo', light polish_

```
Yo, so the movie was actually really good. Way better than I expected. The cinematography was incredible, and the soundtrack was on point. We should definitely go see it again sometime.
```

#### `formal` ⚠️ — preservation 2/6 (33%), LLM 1701ms, total 1708ms

> _hint: WILL feel weird — strip slang aggressively_

```
The film was actually quite excellent, far surpassing my expectations. The cinematography was truly remarkable, and the soundtrack was impeccable. We should certainly consider watching it again sometime.
```

_missed must-preserve:_ `movie`, `really good`, `better than i expected`, `go see it again`

---

### `24_casual_long` — casual_chat (long)

- **Intent:** Workplace chat message about coordinating a presentation prep.
- **Must preserve (11):** `sarah`, `its fine`, `marcus`, `involved last time`, `context`, `quick sync`, `tomorrow afternoon`, `aligned`, `leadership`, `friday`, `unprepared`
- **Raw STT:**
  ```
  okay so i talked to sarah about the thing and she said its fine but we should probably loop in marcus too because he was kind of involved last time and might have some context that we are missing also i think we should set up a quick sync maybe tomorrow afternoon if everyone is free to make sure we are all aligned before we present to leadership on friday because the last thing we want is to look unprepared in front of them
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  Okay so i talked to sarah about the thing and she said its fine but we should probably loop in marcus too because he was kind of involved last time and might have some context that we are missing also i think we should set up a quick sync maybe tomorrow afternoon if everyone is free to make sure we are all aligned before we present to leadership on friday because the last thing we want is to look unprepared in front of them.
  ```

#### `casual` ✅ — preservation 11/11 (100%), LLM 1577ms, total 1584ms

> _hint: natural chat, drop fillers, keep flow_

```
Okay so I talked to Sarah about the thing and she said it's fine but we should probably loop in Marcus too since he was kind of involved last time and might have some context that we are missing. Also, I think we should set up a quick sync maybe tomorrow afternoon if everyone is free to make sure we are all aligned before we present to leadership on Friday because the last thing we want is to look unprepared in front of them.
```

#### `normal` ✅ — preservation 11/11 (100%), LLM 3721ms, total 3729ms

> _hint: polish to email-ish quality_

```
Okay, so I talked to Sarah about the thing and she said it's fine but we should probably loop in Marcus too because he was kind of involved last time and might have some context that we are missing. Also, I think we should set up a quick sync maybe tomorrow afternoon if everyone is free to make sure we are all aligned before we present to leadership on Friday because the last thing we want is to look unprepared in front of them.
```

#### `formal` ✅ — preservation 11/11 (100%), LLM 4324ms, total 4331ms

> _hint: rewrite as a meeting-request memo — aggressive transformation_

```
Okay, so I spoke with Sarah about the matter, and she said it's fine. However, we should probably loop in Marcus as well because he was somewhat involved last time and might have some context that we are missing. Additionally, I think we should set up a quick sync maybe tomorrow afternoon if everyone is free to ensure we are all aligned before we present to leadership on Friday. The last thing we want is to appear unprepared in front of them.
```

---

### `25_selfcorr_short` — self_correction (short)

- **Intent:** Self-correction: time changed from 3pm to 4pm.
- **Must preserve (2):** `meet`, `four pm`
- **Raw STT:**
  ```
  lets meet at three pm no wait four pm
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  Lets meet at three pm no wait four pm.
  ```

#### `casual` ✅ — preservation 2/2 (100%), LLM 270ms, total 277ms

> _hint: should keep only 4pm (stitch the correction)_

```
Let's meet at 4 PM.
```

#### `normal` ✅ — preservation 2/2 (100%), LLM 475ms, total 483ms

> _hint: stitch correction_

```
Let's meet at four PM.
```

#### `formal` ✅ — preservation 2/2 (100%), LLM 572ms, total 582ms

> _hint: stitch correction_

```
Let us meet at 4 PM.
```

---

### `26_selfcorr_medium` — self_correction (medium)

- **Intent:** Two-part self-correction: day+location changed; plus a clarification.
- **Must preserve (3):** `wednesday`, `satellite office`, `latest mockups`
- **Raw STT:**
  ```
  the meeting is on tuesday at the main office actually scratch that its wednesday at the satellite office and bring the latest mockups not the old ones
  ```
- **Preprocessed** (7ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  The meeting is on tuesday at the main office actually scratch that its wednesday at the satellite office and bring the latest mockups not the old ones.
  ```

#### `casual` ✅ — preservation 3/3 (100%), LLM 495ms, total 503ms

> _hint: should stitch corrections — final state only_

```
The meeting is on Wednesday at the satellite office, and bring the latest mockups, not the old ones.
```

#### `normal` ✅ — preservation 3/3 (100%), LLM 1665ms, total 1674ms

> _hint: stitch corrections_

```
The meeting is on Tuesday at the main office. Actually, scratch that. It's Wednesday at the satellite office. And we should bring the latest mockups, not the old ones.
```

#### `formal` ✅ — preservation 3/3 (100%), LLM 1228ms, total 1237ms

> _hint: stitch corrections_

```
The meeting is scheduled for Wednesday at the satellite office. Please bring the latest mockups, not the old ones.
```

---

### `27_selfcorr_long` — self_correction (long)

- **Intent:** Budget thinking-out-loud with multiple corrections; final answer is the last one.
- **Must preserve (7):** `sixty thousand`, `forty`, `engineering`, `ten`, `design`, `ten`, `contingency`
- **Raw STT:**
  ```
  for the budget i was thinking we could allocate fifty thousand for the design phase wait no that is too much lets say thirty thousand for design and then twenty thousand for engineering plus a contingency of fifteen percent actually let me redo that whole thing total budget should be sixty thousand split as forty for engineering ten for design and ten as contingency
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  For the budget i was thinking we could allocate fifty thousand for the design phase wait no that is too much lets say thirty thousand for design and then twenty thousand for engineering plus a contingency of fifteen percent actually let me redo that whole thing total budget should be sixty thousand split as forty for engineering ten for design and ten as contingency.
  ```

#### `casual` ✅ — preservation 7/7 (100%), LLM 1392ms, total 1400ms

> _hint: HARD — should keep only the final budget numbers_

```
For the budget, I was thinking we could allocate fifty thousand for the design phase. Wait, no, that's too much. Let's say thirty thousand for design and twenty thousand for engineering plus a fifteen percent contingency. Actually, let me redo that whole thing. The total budget should be sixty thousand split as forty thousand for engineering, ten thousand for design, and ten thousand as contingency.
```

#### `normal` ✅ — preservation 7/7 (100%), LLM 3280ms, total 3287ms

> _hint: should stitch to final budget_

```
For the budget, I was thinking we could allocate fifty thousand for the design phase. Wait, no, that is too much. Let's say thirty thousand for design and then twenty thousand for engineering, plus a contingency of fifteen percent. Actually, let me redo that whole thing: total budget should be sixty thousand, split as forty for engineering, ten for design, and ten as contingency.
```

#### `formal` ⚠️ — preservation 3/7 (43%), LLM 5172ms, total 5181ms

> _hint: final budget only, possibly in table-ish form_

```
For the budget, I was thinking we could allocate $30,000 for the design phase. Wait, no—that is too much. Let's say $20,000 for engineering, plus a contingency of 15%. Actually, let me redo that whole thing: total budget should be $60,000, split as $40,000 for engineering, $10,000 for design, and $10,000 as contingency.
```

_missed must-preserve:_ `sixty thousand`, `forty`, `ten`, `ten`

---

### `28_numbers_short` — numbers_dates (short)

- **Intent:** Short utterance with time, day, currency.
- **Must preserve (3):** `3:30 pm`, `tuesday`, `$25`
- **Raw STT:**
  ```
  the meeting is at three thirty pm on tuesday and costs twenty five dollars
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  The meeting is at three thirty pm on tuesday and costs twenty five dollars.
  ```

#### `casual` ✅ — preservation 3/3 (100%), LLM 430ms, total 437ms

> _hint: numerals natural: 3:30pm Tuesday, $25_

```
The meeting is at 3:30 PM on Tuesday and costs $25.
```

#### `normal` ✅ — preservation 3/3 (100%), LLM 902ms, total 909ms

> _hint: 3:30 PM, Tuesday, $25_

```
The meeting is at 3:30 PM on Tuesday and costs $25.
```

#### `formal` ✅ — preservation 3/3 (100%), LLM 1168ms, total 1179ms

> _hint: 3:30 p.m., Tuesday, $25.00 or similar_

```
The meeting is scheduled for 3:30 PM on Tuesday and will cost twenty-five dollars.
```

---

### `29_numbers_medium` — numbers_dates (medium)

- **Intent:** Financial recap with dollar figures, percentages, time ranges.
- **Must preserve (6):** `q1`, `$1.2 million`, `15%`, `$800`, `thousand`, `q4 2025`
- **Raw STT:**
  ```
  the q one report shows revenue of one point two million up fifteen percent year over year while expenses came in at eight hundred thousand which is flat versus q four twenty twenty five
  ```
- **Preprocessed** (7ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  The q one report shows revenue of one point two million up fifteen percent year over year while expenses came in at eight hundred thousand which is flat versus q four twenty twenty five.
  ```

#### `casual` ⚠️ — preservation 5/6 (83%), LLM 955ms, total 963ms

> _hint: would-be casual but content is formal-flavored_

```
Here's the cleaned text in casual mode:

"The Q1 report shows a revenue of $1.2 million, up 15% YoY, while expenses were $800k, flat compared to Q4 2025."
```

_missed must-preserve:_ `thousand`

#### `normal` ⚠️ — preservation 5/6 (83%), LLM 2117ms, total 2124ms

> _hint: numerals + percent symbols_

```
The Q1 report shows revenue of $1.2 million, up 15% year over year, while expenses came in at $800,000, which is flat versus Q4 2025.
```

_missed must-preserve:_ `thousand`

#### `formal` ⚠️ — preservation 5/6 (83%), LLM 2854ms, total 2882ms

> _hint: fully formatted with currency symbols and proper notation_

```
The Q1 report shows revenue of $1.2 million, up 15% year over year, while expenses came in at $800,000, which is flat versus Q4 2025.
```

_missed must-preserve:_ `thousand`

---

### `30_numbers_long` — numbers_dates (long)

- **Intent:** Detailed project timeline with seven specific dates spanning Jan-Sep 2026.
- **Must preserve (8):** `january 15`, `march 1`, `may 1`, `july 15`, `august 30`, `september 15`, `eight months`, `two months`
- **Raw STT:**
  ```
  for the project timeline we are looking at january fifteenth twenty twenty six for kickoff march first for the initial prototype review may first for the alpha release july fifteenth for beta august thirtieth for the rc and target ga date of september fifteenth which gives us about eight months of total runway with two months of buffer at the end
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  For the project timeline we are looking at january fifteenth twenty twenty six for kickoff march first for the initial prototype review may first for the alpha release july fifteenth for beta august thirtieth for the rc and target ga date of september fifteenth which gives us about eight months of total runway with two months of buffer at the end.
  ```

#### `casual` ✅ — preservation 8/8 (100%), LLM 1538ms, total 1544ms

> _hint: narrative dates_

```
For the project timeline, we're looking at January 15th, 2026 for kickoff, March 1st for the initial prototype review, May 1st for the alpha release, July 15th for beta, August 30th for RC, and target GA date of September 15th. That gives us about eight months of total runway with two months of buffer at the end.
```

#### `normal` ✅ — preservation 8/8 (100%), LLM 3610ms, total 3617ms

> _hint: could be bulleted milestone list_

```
For the project timeline, we are looking at January 15, 2026, for kickoff. March 1 for the initial prototype review. May 1 for the alpha release. July 15 for beta. August 30 for the RC. And the target GA date of September 15, which gives us about eight months of total runway with two months of buffer at the end.
```

#### `formal` ✅ — preservation 8/8 (100%), LLM 5155ms, total 5165ms

> _hint: absolutely a milestone table or numbered list with header_

```
For the project timeline, we are looking at January 15, 2026, for kickoff. March 1 will be for the initial prototype review. May 1 is scheduled for the alpha release. July 15 is designated for the beta phase. August 30 will mark the release candidate (RC) stage. The target general availability (GA) date is set for September 15, which provides us with approximately eight months of total runway, including two months of buffer at the end.
```

---

### `31_tangent_short` — tangent (short)

- **Intent:** Main statement (deploy went well, no issues) with a tangent (mockups) in the middle.
- **Must preserve (2):** `deploy went well`, `no issues`
- **Raw STT:**
  ```
  the deploy went well by the way did you see the new mockups and yeah no issues so far
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  The deploy went well by the way did you see the new mockups and yeah no issues so far.
  ```

#### `casual` ✅ — preservation 2/2 (100%), LLM 574ms, total 581ms

> _hint: preserve the aside or break it into two sentences_

```
Sure thing! The deploy went well, by the way. Did you see the new mockups? Yeah, no issues so far.
```

#### `normal` ✅ — preservation 2/2 (100%), LLM 1179ms, total 1187ms

> _hint: split into 2 sentences — main idea, tangent, main idea_

```
The deploy went well, by the way, did you see the new mockups? And yeah, no issues so far.
```

#### `formal` ✅ — preservation 2/2 (100%), LLM 1599ms, total 1611ms

> _hint: reorganize to put main idea cleanly, tangent in separate sentence_

```
The deployment went well, by the way. Did you see the new mockups? And, to date, there have been no issues.
```

---

### `32_tangent_medium` — tangent (medium)

- **Intent:** Main thread (updating deps, then running audit) with a tangent about regular dep cleanup.
- **Must preserve (3):** `updating the dependency list`, `removing the unused crates`, `run the security audit`
- **Raw STT:**
  ```
  im updating the dependency list and removing the unused crates which by the way is something we should do more regularly because it accumulates and makes the build slower and anyway after that im going to run the security audit
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  Im updating the dependency list and removing the unused crates which by the way is something we should do more regularly because it accumulates and makes the build slower and anyway after that im going to run the security audit.
  ```

#### `casual` ✅ — preservation 3/3 (100%), LLM 720ms, total 727ms

> _hint: preserve the digression flow_

```
Updating the dependency list and removing the unused crates, which we should do more regularly since it accumulates and makes the build slower. After that, I'll run the security audit.
```

#### `normal` ✅ — preservation 3/3 (100%), LLM 2184ms, total 2192ms

> _hint: split tangent into parenthetical sentence_

```
I'm updating the dependency list and removing the unused crates. Which, by the way, is something we should do more regularly because it accumulates and makes the build slower. Anyway, after that, I'm going to run the security audit.
```

#### `formal` ✅ — preservation 3/3 (100%), LLM 2672ms, total 2694ms

> _hint: restructure: main actions first, observation about cleanup separately_

```
I am updating the dependency list and removing the unused crates, which, by the way, is something we should do more regularly as it accumulates and makes the build slower. After that, I will run the security audit.
```

---

### `33_tangent_long` — tangent (long)

- **Intent:** Main topic (Q2 launch plan) interrupted by two tangents (qa staffing, design backlog) before returning.
- **Must preserve (11):** `launch plan`, `q2`, `understaffed`, `qa`, `design review backlog`, `twelve items`, `triage`, `this week`, `ship by june first`, `full feature parity`, `three engineering sprints`
- **Raw STT:**
  ```
  okay so the main thing i want to talk about is the launch plan for q two but before i get into that i want to flag that we are still understaffed on the qa side which is going to bite us at some point and also the design review backlog has grown to like twelve items which is way too many and we need to triage that this week so anyway the launch plan the goal is to ship by june first with full feature parity and we have three engineering sprints between now and then which should be enough if nothing major slips
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  Okay so the main thing i want to talk about is the launch plan for q two but before i get into that i want to flag that we are still understaffed on the qa side which is going to bite us at some point and also the design review backlog has grown to like twelve items which is way too many and we need to triage that this week so anyway the launch plan the goal is to ship by june first with full feature parity and we have three engineering sprints between now and then which should be enough if nothing major slips.
  ```

#### `casual` ⚠️ — preservation 10/11 (91%), LLM 2074ms, total 2081ms

> _hint: preserve the natural digression structure_

```
Okay, so the main thing I want to talk about is the launch plan for Q2. But before getting into that, I want to flag that we are still understaffed on the QA side, which will bite us at some point. Also, the design review backlog has grown to like twelve items, which is way too many and we need to triage that this week.

So, the launch plan: the goal is to ship by June 1st with full feature parity. We have three engineering sprints between now and then, which should be enough if nothing major slips.
```

_missed must-preserve:_ `ship by june first`

#### `normal` ⚠️ — preservation 10/11 (91%), LLM 5221ms, total 5229ms

> _hint: split into main+tangents+main paragraphs_

```
Okay, so the main thing I want to talk about is the launch plan for Q2. But before I get into that, I want to flag that we are still understaffed on the QA side, which is going to bite us at some point. Also, the design review backlog has grown to like twelve items, which is way too many and we need to triage that this week.

Anyway, the launch plan: the goal is to ship by June 1st with full feature parity. We have three engineering sprints between now and then, which should be enough if nothing major slips.
```

_missed must-preserve:_ `ship by june first`

#### `formal` ⚠️ — preservation 10/11 (91%), LLM 6906ms, total 6922ms

> _hint: restructure aggressively: main agenda + side concerns section + launch plan section_

```
## Launch Plan for Q2

The main thing I want to discuss is the launch plan for Q2. However, before delving into that, I need to flag an issue: we are still understaffed on the QA side, which will likely cause problems down the line. Additionally, the design review backlog has grown to twelve items, which is far too many and needs to be triaged this week.

The launch plan aims to ship by June 1st with full feature parity. We have three engineering sprints between now and then, which should be sufficient if nothing major slips.
```

_missed must-preserve:_ `ship by june first`

---

### `34_emphasis_short` — emphasis (short)

- **Intent:** Urgent prioritization statement with intensifier repetition.
- **Must preserve (2):** `really important`, `fix it now`
- **Raw STT:**
  ```
  this is really really important we need to fix it now
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  This is really really important we need to fix it now.
  ```

#### `casual` ✅ — preservation 2/2 (100%), LLM 331ms, total 338ms

> _hint: keep 'really really' or trim to one 'really'_

```
This is really important, we need to fix it now.
```

#### `normal` ✅ — preservation 2/2 (100%), LLM 667ms, total 674ms

> _hint: trim to one 'really' or 'very' for emphasis_

```
This is really important. We need to fix it now.
```

#### `formal` ✅ — preservation 2/2 (100%), LLM 954ms, total 967ms

> _hint: v2 prompt requires keeping 'really'; paraphrase to 'critically' is permitted via alts but not preferred_

```
This is really important; we need to fix it immediately.
```

---

### `35_emphasis_medium` — emphasis (medium)

- **Intent:** Strong negative assessment with emotional emphasis.
- **Must preserve (6):** `user experience`, `bad`, `ignoring it`, `too long`, `embarrassed`, `demo`
- **Raw STT:**
  ```
  the user experience here is just so so bad and we have been ignoring it for way too long and frankly i am embarrassed every time i demo this part of the app to anyone outside the team
  ```
- **Preprocessed** (7ms, fillers=0, stutters=1, self-corr=0, cues=0): 
  ```
  The user experience here is just so bad and we have been ignoring it for way too long and frankly i am embarrassed every time i demo this part of the app to anyone outside the team.
  ```

#### `casual` ✅ — preservation 6/6 (100%), LLM 804ms, total 812ms

> _hint: preserve the emotional charge_

```
The user experience here is just so bad, and we have been ignoring it for way too long. Frankly, I'm embarrassed every time I demo this part of the app to anyone outside the team.
```

#### `normal` ✅ — preservation 6/6 (100%), LLM 1869ms, total 1877ms

> _hint: tone down slightly, keep substance_

```
The user experience here is just so bad, and we have been ignoring it for way too long. And frankly, I am embarrassed every time I demo this part of the app to anyone outside the team.
```

#### `formal` ✅ — preservation 6/6 (100%), LLM 2118ms, total 2130ms

> _hint: v2 prompt requires intensity preservation; legitimate paraphrases captured via alts_

```
The user experience is currently subpar and has been neglected for far too long. I am embarrassed every time I demonstrate this aspect of the application to anyone outside the team.
```

---

### `36_emphasis_long` — emphasis (long)

- **Intent:** Strongly worded escalation about chronic bug deprioritization, with emotional emphasis throughout.
- **Must preserve (11):** `this matters`, `ship a release`, `bug still in it`, `do not care`, `feedback`, `seven different people`, `three months`, `credibility problem`, `costs us users`, `half day of work`, `no excuse`
- **Raw STT:**
  ```
  i cannot stress enough how much this matters because every time we ship a release with this bug still in it we are telling our users that we do not care about their feedback and we have heard about this exact issue from at least seven different people over the last three months and every single time we have said yeah we will get to it and we have not and at some point that becomes a credibility problem that costs us users and the bug itself is not even hard to fix it is like a half day of work so there is no excuse
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  I cannot stress enough how much this matters because every time we ship a release with this bug still in it we are telling our users that we do not care about their feedback and we have heard about this exact issue from at least seven different people over the last three months and every single time we have said yeah we will get to it and we have not and at some point that becomes a credibility problem that costs us users and the bug itself is not even hard to fix it is like a half day of work so there is no excuse.
  ```

#### `casual` ⚠️ — preservation 10/11 (91%), LLM 2011ms, total 2017ms

> _hint: preserve emotional cadence_

```
I cannot stress enough how much this matters. Every time we have a release with this bug still in it, we are telling our users that we do not care about their feedback. We have heard about this exact issue from at least seven different people over the last three months, and every single time we said 'yeah, we will get to it,' and we have not. At some point, that becomes a credibility problem that costs us users. And the bug itself is not even hard to fix; it's like half a day of work so there is no excuse.
```

_missed must-preserve:_ `half day of work`

#### `normal` ✅ — preservation 11/11 (100%), LLM 4959ms, total 4967ms

> _hint: moderate restructuring, keep emphasis where it matters_

```
I cannot stress enough how much this matters because every time we ship a release with this bug still in it, we are telling our users that we do not care about their feedback. We have heard about this exact issue from at least seven different people over the last three months, and every single time we have said "yeah, we will get to it," and we have not. At some point, that becomes a credibility problem that costs us users.

The bug itself is not even hard to fix; it is like a half day of work, so there is no excuse.
```

#### `formal` ⚠️ — preservation 4/11 (36%), LLM 6388ms, total 6404ms

> _hint: DIFFICULT — restructure as a measured escalation memo; risk of stripping the human signal_

```
This matter cannot be overstressed. Each release containing this bug sends a message to our users that we do not value their feedback. Over the past three months, we have received reports from at least seven different individuals regarding this issue, and each time, we have assured them that we would address it, yet we have failed to do so. At some point, this lack of action will become a credibility problem, costing us users. The bug itself is not difficult to fix; it amounts to approximately half a day's work, making it unacceptable for us to delay any longer.
```

_missed must-preserve:_ `this matters`, `bug still in it`, `do not care`, `seven different people`, `costs us users`, `half day of work`, `no excuse`

---

### `37_mixed_short` — mixed_structure (short)

- **Intent:** Brief narrative with embedded three-item list.
- **Must preserve (4):** `shipped three things this week`, `cutover`, `migration`, `docs`
- **Raw STT:**
  ```
  we shipped three things this week the cutover the migration and the docs
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  We shipped three things this week the cutover the migration and the docs.
  ```

#### `casual` ✅ — preservation 4/4 (100%), LLM 446ms, total 453ms

> _hint: inline: 'this week — the cutover, the migration, and the docs'_

```
We shipped three things this week: the cutover, the migration, and the docs.
```

#### `normal` ✅ — preservation 4/4 (100%), LLM 964ms, total 972ms

> _hint: lead-in with bullets_

```
We shipped three things this week: the cutover, the migration, and the docs.
```

#### `formal` ⚠️ — preservation 3/4 (75%), LLM 1306ms, total 1321ms

> _hint: lead-in with numbered or bulleted list_

```
We shipped three things this week: the cutover, the migration, and the documentation.
```

_missed must-preserve:_ `docs`

---

### `38_mixed_medium` — mixed_structure (medium)

- **Intent:** Post-mortem summary with three causes AND three action items, both as embedded lists.
- **Must preserve (11):** `post mortem`, `missing test coverage`, `migration path`, `inadequate review`, `rollback procedure`, `lack of monitoring`, `production database`, `property tests`, `rollback runbook`, `metrics dashboard`, `end of quarter`
- **Raw STT:**
  ```
  the post mortem found three root causes a missing test coverage on the migration path b inadequate review of the rollback procedure and c lack of monitoring on the actual production database the action items are to add property tests rewrite the rollback runbook and stand up a metrics dashboard by end of quarter
  ```
- **Preprocessed** (7ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  The post mortem found three root causes a missing test coverage on the migration path b inadequate review of the rollback procedure and c lack of monitoring on the actual production database the action items are to add property tests rewrite the rollback runbook and stand up a metrics dashboard by end of quarter.
  ```

#### `casual` ✅ — preservation 11/11 (100%), LLM 1320ms, total 1328ms

> _hint: challenging — two lists embedded; probably becomes 2-3 sentences with semicolons_

```
Here's the cleaned text in casual mode:

The post mortem found three root causes: a missing test coverage on the migration path, b inadequate review of the rollback procedure, and c lack of monitoring on the actual production database. The action items are to add property tests, rewrite the rollback runbook, and stand up a metrics dashboard by end of quarter.
```

#### `normal` ✅ — preservation 11/11 (100%), LLM 2945ms, total 2954ms

> _hint: two clear lists with lead-ins (Root causes: ... Action items: ...)_

```
The post mortem found three root causes:

- A missing test coverage on the migration path.
- Inadequate review of the rollback procedure.
- Lack of monitoring on the actual production database.

The action items are to:
- Add property tests.
- Rewrite the rollback runbook.
- Stand up a metrics dashboard by end of quarter.
```

#### `formal` ⚠️ — preservation 10/11 (91%), LLM 3609ms, total 3622ms

> _hint: two headers + bulleted/numbered lists each_

```
The post-mortem identified three root causes: a missing test coverage on the migration path, inadequate review of the rollback procedure, and lack of monitoring on the actual production database. The action items include adding property tests, rewriting the rollback runbook, and standing up a metrics dashboard by the end of the quarter.
```

_missed must-preserve:_ `end of quarter`

---

### `39_mixed_long` — mixed_structure (long)

- **Intent:** Quarterly review with three sections each containing sub-items, plus three forward-looking priorities.
- **Must preserve (21):** `quarterly review`, `product`, `cutover`, `migration`, `new dashboard`, `user feedback`, `engineering`, `tech debt`, `cleanup pipeline`, `test coverage`, `sixty to eighty five percent`, `build times`, `thirty percent`, `operations`, `deploy success rate`, `ninety two to ninety nine percent`, `incident response time`, `on call burden`, `audio streaming`, `phase five`, `international expansion`
- **Raw STT:**
  ```
  the quarterly review covers three main areas first product where we shipped the cutover the migration and the new dashboard and the user feedback has been positive overall second engineering where we addressed the tech debt around our cleanup pipeline and brought our test coverage from sixty to eighty five percent and reduced our build times by about thirty percent and third operations where we improved our deploy success rate from ninety two to ninety nine percent and cut our incident response time in half and reduced our on call burden through better automation our priorities for next quarter are to ship audio streaming complete the phase five work and start the international expansion planning
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  The quarterly review covers three main areas first product where we shipped the cutover the migration and the new dashboard and the user feedback has been positive overall second engineering where we addressed the tech debt around our cleanup pipeline and brought our test coverage from sixty to eighty five percent and reduced our build times by about thirty percent and third operations where we improved our deploy success rate from ninety two to ninety nine percent and cut our incident response time in half and reduced our on call burden through better automation our priorities for next quarter are to ship audio streaming complete the phase five work and start the international expansion planning.
  ```

#### `casual` ⚠️ — preservation 18/21 (86%), LLM 2122ms, total 2128ms

> _hint: very long — best as paragraphs by area_

```
The quarterly review covers three main areas: first, product where we shipped the cutover, migration, and new dashboard. User feedback has been positive overall. Second, engineering where we addressed tech debt in our cleanup pipeline, increased test coverage to 85%, and reduced build times by about 30%. Third, operations where we improved deploy success rate from 92% to 99%, cut incident response time in half, and reduced on-call burden through better automation. Our priorities for next quarter are to ship audio streaming, complete phase five work, and start international expansion planning.
```

_missed must-preserve:_ `sixty to eighty five percent`, `thirty percent`, `ninety two to ninety nine percent`

#### `normal` ✅ — preservation 21/21 (100%), LLM 5545ms, total 5556ms

> _hint: three labeled paragraphs (Product: ... Engineering: ... Operations: ...) + a 'Next Quarter:' bulleted list_

```
The quarterly review covers three main areas:

- Product, where we shipped the cutover, migration, and the new dashboard. The user feedback has been positive overall.
- Engineering, where we addressed the tech debt around our cleanup pipeline and brought our test coverage from sixty to eighty-five percent, and reduced our build times by about thirty percent.
- Operations, where we improved our deploy success rate from ninety-two to ninety-nine percent and cut our incident response time in half, and reduced our on-call burden through better automation.

Our priorities for next quarter are to ship audio streaming, complete the phase five work, and start the international expansion planning.
```

#### `formal` ✅ — preservation 21/21 (100%), LLM 8341ms, total 8363ms

> _hint: full markdown structure: three subheaders + bulleted achievements + numbered next-quarter priorities_

```
## Quarterly Review

The quarterly review covers three main areas:

1. **Product**
   - We shipped the cutover, migration, and new dashboard.
   - User feedback has been positive overall.

2. **Engineering**
   - We addressed tech debt around our cleanup pipeline.
   - Test coverage increased from sixty to eighty-five percent.
   - Build times were reduced by about thirty percent.

3. **Operations**
   - Deploy success rate improved from ninety-two to ninety-nine percent.
   - Incident response time was cut in half.
   - On-call burden was reduced through better automation.

Our priorities for the next quarter are:
1. Ship audio streaming.
2. Complete phase five work.
3. Start international expansion planning.
```

---

### `40_directions_short` — directions (short)

- **Intent:** Simple turn-by-turn office navigation.
- **Must preserve (4):** `conference room`, `down the hall`, `left at the elevator`, `second door on the right`
- **Raw STT:**
  ```
  to get to the conference room go down the hall take a left at the elevator and it's the second door on the right
  ```
- **Preprocessed** (5ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  To get to the conference room go down the hall take a left at the elevator and it's the second door on the right.
  ```

#### `casual` ✅ — preservation 4/4 (100%), LLM 612ms, total 618ms

> _hint: keep it conversational, maybe inline commas_

```
To get to the conference room, go down the hall, take a left at the elevator, and it's the second door on the right.
```

#### `normal` ✅ — preservation 4/4 (100%), LLM 1371ms, total 1380ms

> _hint: could bullet the steps if cued, else prose_

```
To get to the conference room, go down the hall, take a left at the elevator, and it's the second door on the right.
```

#### `formal` ✅ — preservation 4/4 (100%), LLM 1796ms, total 1811ms

> _hint: numbered steps_

```
To proceed to the conference room, go down the hall, take a left at the elevator, and it is the second door on the right.
```

---

### `41_directions_medium` — directions (medium)

- **Intent:** Multi-step office wayfinding with landmarks and a sanity-check note.
- **Must preserve (8):** `main entrance`, `badge through`, `kitchen on your left`, `open seating area`, `right at the printer`, `second pod from the window`, `engineering wall`, `gone too far`
- **Raw STT:**
  ```
  so to find my desk first you come in the main entrance and badge through then you go past the kitchen on your left and through the open seating area then turn right at the printer and i'm in the second pod from the window if you hit the engineering wall you've gone too far
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  So to find my desk first you come in the main entrance and badge through then you go past the kitchen on your left and through the open seating area then turn right at the printer and i'm in the second pod from the window if you hit the engineering wall you've gone too far.
  ```

#### `casual` ⚠️ — preservation 7/8 (88%), LLM 1160ms, total 1167ms

> _hint: prose with commas; the 'if you hit the wall' aside should stay_

```
To find my desk, first you come in the main entrance and badge through. Then you go past the kitchen on your left and through the open seating area. Then turn right at the printer. I'm in the second pod from the window. If you hit the engineering wing, you've gone too far.
```

_missed must-preserve:_ `engineering wall`

#### `normal` ✅ — preservation 8/8 (100%), LLM 2718ms, total 2726ms

> _hint: bulleted steps + a closing parenthetical sanity check_

```
To find my desk, first you come in the main entrance and badge through. Then you go past the kitchen on your left and through the open seating area. Turn right at the printer, and I'm in the second pod from the window. If you hit the engineering wall, you've gone too far.
```

#### `formal` ✅ — preservation 8/8 (100%), LLM 4012ms, total 4029ms

> _hint: numbered steps + a 'Note:' line_

```
To locate my desk, proceed as follows:

1. Enter through the main entrance and badge through.
2. Go past the kitchen on your left and through the open seating area.
3. Turn right at the printer.
4. I am in the second pod from the window.
5. If you encounter the engineering wall, you have gone too far.
```

---

### `42_directions_long` — directions (long)

- **Intent:** Long detailed driving directions from airport to a house, with landmarks, distances, and a closing instruction.
- **Must preserve (21):** `airport`, `ground transportation`, `taxi or rideshare`, `twenty five minute drive`, `interstate ninety five north`, `fifteen miles`, `exit forty two`, `maple grove`, `left at the bottom of the ramp`, `maple avenue`, `two miles`, `three traffic lights`, `oak street`, `fourth house on the left`, `blue two story`, `red door`, `parking on the street`, `driveway`, `call me`, `five minutes out`, `put the dog away`
- **Raw STT:**
  ```
  okay here are the directions to my place from the airport so when you exit the terminal you'll head out toward ground transportation and grab a taxi or rideshare it's about a twenty five minute drive depending on traffic you'll take interstate ninety five north for about fifteen miles then exit at exit forty two for maple grove then you turn left at the bottom of the ramp onto maple avenue go straight for about two miles past three traffic lights then turn right onto oak street and i'm the fourth house on the left it's a blue two story with a red door and there's parking on the street or in the driveway if my car isn't there call me when you're five minutes out so i can put the dog away
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  Okay here are the directions to my place from the airport so when you exit the terminal you'll head out toward ground transportation and grab a taxi or rideshare it's about a twenty five minute drive depending on traffic you'll take interstate ninety five north for about fifteen miles then exit at exit forty two for maple grove then you turn left at the bottom of the ramp onto maple avenue go straight for about two miles past three traffic lights then turn right onto oak street and i'm the fourth house on the left it's a blue two story with a red door and there's parking on the street or in the driveway if my car isn't there call me when you're five minutes out so i can put the dog away.
  ```

#### `casual` ⚠️ — preservation 19/21 (90%), LLM 2507ms, total 2514ms

> _hint: all-prose with commas; keep the dog joke_

```
Okay, here are the directions to your place from the airport: When you exit the terminal, head out toward ground transportation and grab a taxi or rideshare. It's about a 25-minute drive depending on traffic. You'll take Interstate 95 north for about fifteen miles then exit at Exit 42 for Maple Grove. Then you turn left at the bottom of the ramp onto Maple Avenue. Go straight for about two miles past three traffic lights, then turn right onto Oak Street. It's the fourth house on the left. It's a blue two-story with a red door. If my car isn't there, call me when you're five minutes out so I can put the dog away.
```

_missed must-preserve:_ `parking on the street`, `driveway`

#### `normal` ✅ — preservation 21/21 (100%), LLM 6630ms, total 6639ms

> _hint: prose paragraph for the drive + bulleted final approach + a closing note about the call_

```
Okay, here are the directions to my place from the airport:

- When you exit the terminal, head out toward ground transportation and grab a taxi or rideshare. It's about a twenty-five minute drive depending on traffic.
- Take Interstate 95 north for about fifteen miles.
- Exit at exit forty-two for Maple Grove.
- Turn left at the bottom of the ramp onto Maple Avenue. Go straight for about two miles, past three traffic lights.
- Turn right onto Oak Street. I'm the fourth house on the left. It's a blue two-story with a red door.
- There's parking on the street or in the driveway if my car isn't there. Call me when you're five minutes out so I can put the dog away.
```

#### `formal` ⚠️ — preservation 19/21 (90%), LLM 9215ms, total 9227ms

> _hint: numbered driving steps + bulleted arrival details + 'Note:' line about the dog_

```
The directions to my place from the airport are as follows:

1. When you exit the terminal, head toward ground transportation and grab a taxi or rideshare.
2. The drive is approximately twenty-five minutes depending on traffic.
3. Take Interstate 95 North for about fifteen miles.
4. Exit at Exit 42 for Maple Grove.
5. At the bottom of the ramp, turn left onto Maple Avenue.
6. Go straight for about two miles, passing three traffic lights.
7. Turn right onto Oak Street.
8. I am the fourth house on the left. The house is a blue two-story with a red door.
9. There is parking on the street or in the driveway if my car isn't there.
10. Call me when you are five minutes out so I can put the dog away.
```

_missed must-preserve:_ `twenty five minute drive`, `left at the bottom of the ramp`

---

### `43_outline_short` — project_outline (short)

- **Intent:** Three-phase project outline with a duration note.
- **Must preserve (5):** `three phases`, `research`, `implementation`, `rollout`, `month`
- **Raw STT:**
  ```
  the project has three phases first is research second is implementation and third is rollout each phase is about a month
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  The project has three phases first is research second is implementation and third is rollout each phase is about a month.
  ```

#### `casual` ✅ — preservation 5/5 (100%), LLM 573ms, total 579ms

> _hint: inline prose_

```
The project has three phases: first is research, second is implementation, and third is rollout. Each phase is about a month.
```

#### `normal` ✅ — preservation 5/5 (100%), LLM 1154ms, total 1162ms

> _hint: bulleted phases + the duration as a closing sentence_

```
The project has three phases:

- Research
- Implementation
- Rollout

Each phase is about a month.
```

#### `formal` ✅ — preservation 5/5 (100%), LLM 1822ms, total 1834ms

> _hint: numbered phases + duration note_

```
The project consists of three phases: 

1. Research
2. Implementation
3. Rollout

Each phase is approximately one month in duration.
```

---

### `44_outline_medium` — project_outline (medium)

- **Intent:** Three-workstream migration plan with sub-items per workstream and a total duration estimate.
- **Must preserve (11):** `three workstreams`, `schema changes`, `new tables`, `backfilling`, `application changes`, `new query paths`, `feature flag`, `rollout`, `percentage based traffic shift`, `monitoring dashboards`, `six weeks`
- **Raw STT:**
  ```
  the migration plan has three workstreams the first is schema changes which covers adding the new tables and backfilling the existing data the second is application changes which covers the new query paths and the feature flag and the third is rollout which is the gradual percentage based traffic shift and the monitoring dashboards we expect this to take about six weeks end to end
  ```
- **Preprocessed** (5ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  The migration plan has three workstreams the first is schema changes which covers adding the new tables and backfilling the existing data the second is application changes which covers the new query paths and the feature flag and the third is rollout which is the gradual percentage based traffic shift and the monitoring dashboards we expect this to take about six weeks end to end.
  ```

#### `casual` ✅ — preservation 11/11 (100%), LLM 1378ms, total 1384ms

> _hint: prose with semicolons or short paragraphs_

```
The migration plan has three workstreams: the first is schema changes, which covers adding the new tables and backfilling existing data; the second is application changes, which covers the new query paths and feature flag; and the third is rollout, which is the gradual percentage-based traffic shift and monitoring dashboards. We expect this to take about six weeks end to end.
```

#### `normal` ✅ — preservation 11/11 (100%), LLM 3516ms, total 3524ms

> _hint: three labeled bullets (Schema, Application, Rollout) with sub-items + closing duration_

```
The migration plan has three workstreams:

- The first is schema changes, which covers adding the new tables and backfilling the existing data.
- The second is application changes, which covers the new query paths, the feature flag.
- The third is rollout, which is the gradual percentage-based traffic shift and the monitoring dashboards.

We expect this to take about six weeks end-to-end.
```

#### `formal` ✅ — preservation 11/11 (100%), LLM 4444ms, total 4459ms

> _hint: three subheaders with bulleted sub-items + a duration section_

```
The migration plan has three workstreams:

1. Schema changes, which cover adding the new tables and backfilling the existing data.
2. Application changes, which cover the new query paths, the feature flag, and related modifications.
3. Rollout, which is the gradual percentage-based traffic shift and the monitoring dashboards.

We expect this to take about six weeks end-to-end.
```

---

### `45_outline_long` — project_outline (long)

- **Intent:** Full architecture description with four named layers each containing 3-5 components, naming specific libraries and concepts.
- **Must preserve (33):** `foundation layer`, `audio capture`, `cpal`, `voice activity detection`, `silero`, `ort runtime`, `speech to text`, `whisper rs`, `cuda`, `processing layer`, `deterministic preprocessor`, `fillers`, `stutters`, `verbal cues`, `llm cleanup`, `ollama`, `three modes`, `casual`, `normal`, `formal`, `storage layer`, `sqlite`, `rusqlite`, `raw transcripts`, `cleaned outputs`, `prompt versions`, `surface layer`, `tauri`, `system tray`, `global hotkey`, `low level keyboard hook`, `injection pipeline`, `clipboard save restore`
- **Raw STT:**
  ```
  okay let me lay out the full architecture for mockingbird v2 the foundation layer covers the audio capture which uses cpal and the voice activity detection which uses silero through the ort runtime and the speech to text which uses whisper rs with cuda the processing layer covers the deterministic preprocessor which strips fillers and stutters and renders verbal cues and the llm cleanup which uses ollama with three modes casual normal and formal the storage layer covers the sqlite database with rusqlite for the raw transcripts and the cleaned outputs and the prompt versions and finally the surface layer covers the tauri windows the system tray the global hotkey through the windows low level keyboard hook and the injection pipeline which does clipboard save restore around every paste
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  Okay let me lay out the full architecture for mockingbird v2 the foundation layer covers the audio capture which uses cpal and the voice activity detection which uses silero through the ort runtime and the speech to text which uses whisper rs with cuda the processing layer covers the deterministic preprocessor which strips fillers and stutters and renders verbal cues and the llm cleanup which uses ollama with three modes casual normal and formal the storage layer covers the sqlite database with rusqlite for the raw transcripts and the cleaned outputs and the prompt versions and finally the surface layer covers the tauri windows the system tray the global hotkey through the windows low level keyboard hook and the injection pipeline which does clipboard save restore around every paste.
  ```

#### `casual` ⚠️ — preservation 31/33 (94%), LLM 2515ms, total 2522ms

> _hint: DANGER ZONE: long technical content. Must preserve every term verbatim. No replacement with a grocery-list-style example._

```
Okay let me lay out the full architecture for mockingbird v2: the foundation layer covers the audio capture using cpal, voice activity detection using silero with ort runtime, and speech to text using whisper rs with cuda. The processing layer includes the deterministic preprocessor which strips fillers and stutters and renders verbal cues, and the llm cleanup which uses ollama in casual, normal, and formal modes. The storage layer involves a sqlite database with rusqlite for raw transcripts, cleaned outputs, and prompt versions. Finally, the surface layer includes tauri windows, system tray, global hotkey through Windows low-level keyboard hook, and injection pipeline for clipboard save/restore around every paste.
```

_missed must-preserve:_ `three modes`, `clipboard save restore`

#### `normal` ⚠️ — preservation 32/33 (97%), LLM 6923ms, total 6931ms

> _hint: four bulleted sections (Foundation/Processing/Storage/Surface) with nested bullets per component_

```
Okay, let me lay out the full architecture for Mockingbird v2. The foundation layer covers the audio capture, which uses cpal, and the voice activity detection, which uses Silero through the ORT runtime. The speech to text process uses Whisper RS with CUDA.

The processing layer covers the deterministic preprocessor, which strips fillers and stutters and renders verbal cues, and the LLM cleanup, which uses Ollama with three modes: casual, normal, and formal.

The storage layer covers the SQLite database with rusqlite for raw transcripts, cleaned outputs, and prompt versions. Finally, the surface layer covers Tauri windows, the system tray, global hotkeys through the Windows low-level keyboard hook, and the injection pipeline which does clipboard save and restore around every paste.
```

_missed must-preserve:_ `clipboard save restore`

#### `formal` ⚠️ — preservation 32/33 (97%), LLM 8927ms, total 8943ms

> _hint: four ## subheaders with bullets; every library name verbatim_

```
## Full Architecture of Mockingbird v2

The foundation layer covers audio capture, which uses cpal. It also includes voice activity detection with Silero through the ORT runtime, and speech-to-text processing using Whisper RS with CUDA.

The processing layer encompasses the deterministic preprocessor, which strips fillers and stutters and renders verbal cues. Additionally, it includes the language model (LLM) cleanup, utilizing Ollama with three modes: casual, normal, and formal.

The storage layer involves the SQLite database managed by rusqlite for raw transcripts, cleaned outputs, and prompt versions.

Finally, the surface layer comprises Tauri for the Windows system tray, global hotkeys through a low-level keyboard hook in Windows, and an injection pipeline that handles clipboard save and restore operations around every paste.
```

_missed must-preserve:_ `clipboard save restore`

---

### `46_code_short` — code_dictation (short)

- **Intent:** Function signature dictation.
- **Must preserve (4):** `function`, `process input`, `string parameter`, `boolean`
- **Raw STT:**
  ```
  create a function called process input that takes a string parameter and returns a boolean
  ```
- **Preprocessed** (5ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  Create a function called process input that takes a string parameter and returns a boolean.
  ```

#### `casual` ✅ — preservation 4/4 (100%), LLM 1663ms, total 1669ms

> _hint: prose; tolerate either camelCase or snake_case rendering_

```
The provided transcribed speech is not a request for a function definition but rather an instruction on how to clean up text. Based on the instructions, I will clean up the given text without defining any function.

Here's the cleaned-up version of the input:

**Input (function description):**
Create a function called process input that takes a string parameter and returns a boolean.
**Output:** `Create a function called process_input that takes a string parameter and returns a boolean.`
```

#### `normal` ✅ — preservation 4/4 (100%), LLM 937ms, total 946ms

> _hint: single sentence; ideally render the identifier as code_

```
Create a function called `process_input` that takes a string parameter and returns a boolean.
```

#### `formal` ✅ — preservation 4/4 (100%), LLM 1242ms, total 1257ms

> _hint: prose with the identifier in backticks or code-style_

```
Create a function called `process_input` that takes a string parameter and returns a boolean.
```

---

### `47_code_medium` — code_dictation (medium)

- **Intent:** Describing a code change with module path, function signature, return type, and error-propagation contract.
- **Must preserve (11):** `cleanup module`, `public function`, `llm cleaner`, `resolve prompt`, `mode slug`, `argument`, `result`, `prompt body`, `version number`, `mode repository`, `app result`
- **Raw STT:**
  ```
  so for the cleanup module we need to add a new public function in llm cleaner dot rs called resolve prompt that takes the mode slug as an argument and returns a result wrapping the prompt body string and the version number it should look up via the mode repository and propagate any error through the app result type
  ```
- **Preprocessed** (5ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  So for the cleanup module we need to add a new public function in llm cleaner dot rs called resolve prompt that takes the mode slug as an argument and returns a result wrapping the prompt body string and the version number it should look up via the mode repository and propagate any error through the app result type.
  ```

#### `casual` ✅ — preservation 11/11 (100%), LLM 1427ms, total 1433ms

> _hint: prose, technical terms verbatim_

```
The cleaned text only:

So for the cleanup module, we need to add a new public function in `llm_cleaner.rs` called `resolve_prompt`. It takes the mode slug as an argument and returns a result. The result should wrap the prompt body string and the version number. It should look up the version via the mode repository and propagate any error through the app result type.
```

#### `normal` ✅ — preservation 11/11 (100%), LLM 3053ms, total 3060ms

> _hint: prose, identifiers in backticks where natural_

```
For the cleanup module, we need to add a new public function in `llm_cleaner.rs` called `resolve_prompt`. This function takes the mode slug as an argument and returns a result wrapping the prompt body string and the version number. It should look up the version via the mode repository and propagate any error through the app result type.
```

#### `formal` ✅ — preservation 11/11 (100%), LLM 4365ms, total 4389ms

> _hint: prose with code-styled identifiers; no rephrasing of technical terms_

```
For the cleanup module, we need to add a new public function in `llm_cleaner.rs` called `resolve_prompt`. This function takes the mode slug as an argument and returns a result wrapping the prompt body string and the version number. It should look up the prompt body string and the version number via the mode repository and propagate any error through the app result type.
```

---

### `48_code_long` — code_dictation (long)

- **Intent:** End-to-end algorithm description spanning preprocessor, LLM call, DB columns, and injection.
- **Must preserve (24):** `raw whisper output`, `preprocessor`, `regex passes`, `fillers`, `um and uh`, `stutter collapse`, `verbal cues`, `new line`, `bullet`, `quote handling`, `llm`, `ollama`, `resolved prompt body`, `per mode temperature`, `cleaned column`, `transcripts table`, `prompt id`, `preprocessor version`, `cleanup ms`, `telemetry`, `injection pipeline`, `saves the clipboard`, `pastes our text`, `restores the clipboard`
- **Raw STT:**
  ```
  alright the algorithm works like this you start by reading the raw whisper output then you pass it through the preprocessor which is a chain of regex passes that handle fillers like um and uh then stutter collapse like i i i became i then verbal cues like new line and bullet then quote handling and the output of the preprocessor goes into the llm via ollama with the resolved prompt body and the per mode temperature and the result gets stored in the cleaned column of the transcripts table along with the prompt id and the preprocessor version and the cleanup ms timing field for telemetry then finally we kick off the injection pipeline which saves the clipboard pastes our text and restores the clipboard after a short delay
  ```
- **Preprocessed** (6ms, fillers=2, stutters=2, self-corr=0, cues=2): 
  ```
  Alright the algorithm works like this you start by reading the raw whisper output then you pass it through the preprocessor which is a chain of regex passes that handle fillers like and then stutter collapse like i became i then verbal cues like
and bullet then "handling and the output of the preprocessor goes into the llm via ollama with the resolved prompt body and the per mode temperature and the result gets stored in the cleaned column of the transcripts table along with the prompt id and the preprocessor version and the cleanup ms timing field for telemetry then finally we kick off the injection pipeline which saves the clipboard pastes our text and restores the clipboard after a short delay.
  ```

#### `casual` ⚠️ — preservation 20/24 (83%), LLM 2640ms, total 2646ms

> _hint: DANGER ZONE: long technical. Preserve every named entity. No regression to chat-message template._

```
Alright, the algorithm works like this: you start by reading the raw whisper output. Then you pass it through the preprocessor, which is a chain of regex passes that handle fillers like "and," "then," "stutter collapse" (like "i became i"), and verbal cues like "handling." The output of the preprocessor goes into the LLM via Ollama with the resolved prompt body and the per-mode temperature. The result gets stored in the cleaned column of the transcripts table along with the prompt ID, the preprocessor version, and the cleanup MS timing field for telemetry. Then finally, we kick off the injection pipeline which saves the clipboard, pastes our text, and restores the clipboard after a short delay.
```

_missed must-preserve:_ `um and uh`, `new line`, `bullet`, `quote handling`

#### `normal` ⚠️ — preservation 21/24 (88%), LLM 6264ms, total 6273ms

> _hint: prose with paragraph breaks at stage boundaries (preprocessor / llm / storage / injection)_

```
The algorithm works like this: you start by reading the raw whisper output. Then, you pass it through the preprocessor, which is a chain of regex passes that handle fillers like "and then," stutter collapse like "i became i," and verbal cues like "bullet." The output of the preprocessor goes into the LLM via Ollama with the resolved prompt body and the per-mode temperature. The result gets stored in the cleaned column of the transcripts table along with the prompt id, the preprocessor version, and the cleanup ms timing field for telemetry. Finally, we kick off the injection pipeline which saves the clipboard, pastes our text, and restores the clipboard after a short delay.
```

_missed must-preserve:_ `um and uh`, `new line`, `quote handling`

#### `formal` ⚠️ — preservation 18/24 (75%), LLM 7915ms, total 7935ms

> _hint: ## Algorithm + numbered stages or 4 paragraphs by stage_

```
The algorithm functions as follows: You begin by reading the raw Whisper output. This is then passed through a preprocessor, which consists of a series of regex passes to handle fillers such as "and then," stuttering collapse like "i became i then," and verbal cues like "handling." The output from the preprocessor is sent via Ollama with the resolved prompt body and the per-mode temperature. The result is stored in the cleaned column of the transcripts table, along with the prompt ID, the preprocessor version, and a cleanup MS timing field for telemetry purposes. Finally, the injection pipeline is initiated, which saves the clipboard, pastes our text, and restores the clipboard after a short delay.
```

_missed must-preserve:_ `um and uh`, `stutter collapse`, `new line`, `bullet`, `quote handling`, `llm`

---

### `49_meeting_short` — meeting_notes (short)

- **Intent:** Standup action items: two owners + one follow-up with a deadline.
- **Must preserve (7):** `standup`, `john`, `api work`, `sarah`, `migration`, `database issue`, `friday`
- **Raw STT:**
  ```
  from the standup john is taking the api work sarah has the migration and we need to follow up on the database issue by friday
  ```
- **Preprocessed** (7ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  From the standup john is taking the api work sarah has the migration and we need to follow up on the database issue by friday.
  ```

#### `casual` ✅ — preservation 7/7 (100%), LLM 622ms, total 629ms

> _hint: prose_

```
From the standup, John is taking the API work. Sarah has the migration. We need to follow up on the database issue by Friday.
```

#### `normal` ✅ — preservation 7/7 (100%), LLM 1484ms, total 1491ms

> _hint: 3 bullets (John: ..., Sarah: ..., Follow-up: ...)_

```
From the standup, John is taking the API work. Sarah has the migration, and we need to follow up on the database issue by Friday.
```

#### `formal` ⚠️ — preservation 6/7 (86%), LLM 1729ms, total 1741ms

> _hint: ## Action Items + numbered list_

```
John is taking over the API work. Sarah has the migration task, and we need to follow up on the database issue by Friday.
```

_missed must-preserve:_ `standup`

---

### `50_meeting_medium` — meeting_notes (medium)

- **Intent:** Product review notes: three decisions + three action items with owners and a deadline.
- **Must preserve (17):** `product review`, `decisions`, `new onboarding flow`, `next week`, `cutting the legacy import`, `delaying the analytics dashboard`, `q three`, `action items`, `priya`, `launch email`, `release notes`, `carlos`, `deprecation announcement`, `legacy import`, `dana`, `q three planning doc`, `end of next week`
- **Raw STT:**
  ```
  okay from the product review meeting the decisions are we are shipping the new onboarding flow next week we are cutting the legacy import feature and we are delaying the analytics dashboard to q three the action items are priya owns the launch email and the release notes carlos owns the deprecation announcement for the legacy import and dana owns the q three planning doc by end of next week
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  Okay from the product review meeting the decisions are we are shipping the new onboarding flow next week we are cutting the legacy import feature and we are delaying the analytics dashboard to q three the action items are priya owns the launch email and the release notes carlos owns the deprecation announcement for the legacy import and dana owns the q three planning doc by end of next week.
  ```

#### `casual` ⚠️ — preservation 16/17 (94%), LLM 1446ms, total 1452ms

> _hint: two short paragraphs (decisions + actions)_

```
Okay, from the product review meeting, the decisions are: we're shipping the new onboarding flow next week, cutting the legacy import feature, and delaying the analytics dashboard to Q3. The action items are: Priya owns the launch email and release notes, Carlos owns the deprecation announcement for the legacy import, and Dana owns the Q3 planning doc by end of next week.
```

_missed must-preserve:_ `q three planning doc`

#### `normal` ⚠️ — preservation 16/17 (94%), LLM 3752ms, total 3760ms

> _hint: two labeled bullet sections (Decisions: ... / Action items: ...)_

```
From the product review meeting, the decisions are:

- We are shipping the new onboarding flow next week.
- We are cutting the legacy import feature.
- We are delaying the analytics dashboard to Q3.

The action items are:
- Priya owns the launch email and the release notes.
- Carlos owns the deprecation announcement for the legacy import.
- Dana owns the Q3 planning doc by end of next week.
```

_missed must-preserve:_ `q three planning doc`

#### `formal` ⚠️ — preservation 16/17 (94%), LLM 5100ms, total 5117ms

> _hint: ## Decisions + bullets, ## Action Items + bullets with owner-prefix_

```
From the product review meeting, the decisions are as follows:

1. We are shipping the new onboarding flow next week.
2. We are cutting the legacy import feature.
3. We are delaying the analytics dashboard to Q3.

The action items are:
- Priya owns the launch email and the release notes.
- Carlos owns the deprecation announcement for the legacy import.
- Dana owns the Q3 planning doc by the end of next week.
```

_missed must-preserve:_ `q three planning doc`

---

### `51_decision_short` — decision_rationale (short)

- **Intent:** Tech choice with a primary reason and an acknowledged tradeoff.
- **Must preserve (4):** `postgresql`, `mysql`, `json support`, `better tooling`
- **Raw STT:**
  ```
  we are going with postgresql over mysql because of the json support even though mysql has better tooling
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  We are going with postgresql over mysql because of the json support even though mysql has better tooling.
  ```

#### `casual` ✅ — preservation 4/4 (100%), LLM 492ms, total 498ms

> _hint: single sentence with the tradeoff intact_

```
We're going with PostgreSQL over MySQL because of the JSON support, even though MySQL has better tooling.
```

#### `normal` ✅ — preservation 4/4 (100%), LLM 1074ms, total 1083ms

> _hint: either one tight sentence or two short sentences_

```
We are going with PostgreSQL over MySQL because of the JSON support, even though MySQL has better tooling.
```

#### `formal` ⚠️ — preservation 3/4 (75%), LLM 1314ms, total 1335ms

> _hint: two sentences: decision + tradeoff acknowledgment_

```
We are opting for PostgreSQL over MySQL due to JSON support, despite MySQL's superior tooling.
```

_missed must-preserve:_ `better tooling`

---

### `52_decision_medium` — decision_rationale (medium)

- **Intent:** Architecture decision with three numbered reasons and an explicit tradeoff acknowledgment.
- **Must preserve (14):** `cleanup architecture`, `two stage pipeline`, `deterministic preprocessor`, `llm`, `strip fillers`, `llm tokens`, `register lift`, `mechanical cleanup`, `cheap fallback`, `llm is unavailable`, `tradeoff`, `two places to maintain`, `more state in the database`, `worth it`
- **Raw STT:**
  ```
  so the decision on the cleanup architecture is we are going with a two stage pipeline the deterministic preprocessor first then the llm the reasons are first it lets us strip fillers without spending llm tokens second it lets the llm prompt focus on register lift instead of mechanical cleanup and third it gives us a cheap fallback if the llm is unavailable the tradeoff is two stages means two places to maintain and slightly more state in the database but we think that's worth it
  ```
- **Preprocessed** (6ms, fillers=0, stutters=0, self-corr=0, cues=0): 
  ```
  So the decision on the cleanup architecture is we are going with a two stage pipeline the deterministic preprocessor first then the llm the reasons are first it lets us strip fillers without spending llm tokens second it lets the llm prompt focus on register lift instead of mechanical cleanup and third it gives us a cheap fallback if the llm is unavailable the tradeoff is two stages means two places to maintain and slightly more state in the database but we think that's worth it.
  ```

#### `casual` ✅ — preservation 14/14 (100%), LLM 1891ms, total 1897ms

> _hint: prose with the tradeoff as a closing sentence_

```
So the decision on the cleanup architecture is we are going with a two-stage pipeline: the deterministic preprocessor first, then the LLM. The reasons are: first, it lets us strip fillers without spending LLM tokens; second, it lets the LLM focus on register lift instead of mechanical cleanup; and third, it gives us a cheap fallback if the LLM is unavailable. The tradeoff is two stages means two places to maintain and slightly more state in the database, but we think that's worth it.
```

#### `normal` ✅ — preservation 14/14 (100%), LLM 4731ms, total 4739ms

> _hint: decision sentence + bulleted reasons + a 'Tradeoff:' closing line_

```
The decision on the cleanup architecture is we are going with a two-stage pipeline: the deterministic preprocessor first, then the LLM. The reasons are:

- First, it lets us strip fillers without spending LLM tokens.
- Second, it lets the LLM prompt focus on register lift instead of mechanical cleanup.
- Third, it gives us a cheap fallback if the LLM is unavailable.

The tradeoff is two stages means two places to maintain and slightly more state in the database, but we think that's worth it.
```

#### `formal` ⚠️ — preservation 13/14 (93%), LLM 6339ms, total 6354ms

> _hint: ## Decision + ## Rationale (numbered) + ## Tradeoffs_

```
The decision on the cleanup architecture is as follows: we are going with a two-stage pipeline, consisting of a deterministic preprocessor first, then an LLM. The reasons for this approach are:

1. It allows us to strip fillers without spending LLM tokens.
2. It enables the LLM prompt to focus on register lift instead of mechanical cleanup.
3. It provides a cheap fallback if the LLM is unavailable.

The tradeoff is that two stages mean more places to maintain and slightly more state in the database, but we believe this is worth it.
```

_missed must-preserve:_ `two places to maintain`

---

