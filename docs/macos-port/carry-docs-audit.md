# macOS-port carry-docs audit

Generated by Phase 0 Leaf 0.5 (`code-puppy-9e98aa`, 2026-06-19).

## RE-AUDIT after maintainer carry bundle (`code-puppy-edf086`, 2026-06-19)

Bundle `mockingbird-maintainer-carry-20260619-104123.tar.gz`
(sha256 `0089a28b9a672cd2f401947b59143ab43194ce50159c540b67f58b9757626b23`,
no sidecar) extracted into repo root. Re-verification of the 13 items:

| Item                                             | Re-audit | Notes |
|--------------------------------------------------|----------|-------|
| `.code_puppy/`                                   | PRESENT  | full tree extracted |
| `.code_puppy/judges-template.json`               | PRESENT  | unblocks Leaf 0.3 |
| `.code_puppy/hooks.toml`                          | N/A      | not carried; hook layer on this box is `.code_puppy/settings.json` (present, 2.4KB) per AGENTS.md. `hooks.toml` was "optional but expected" — superseded, not a blocker. |
| `PLAN-mockingbird-v2.md`                          | PRESENT  | |
| `STATUS.md`                                       | PRESENT  | goal-pin target for Leaf 0.6 |
| `AGENTS.md`                                       | PRESENT* | canonical AGENTS.md arrived at `.code_puppy/AGENTS.md` (506 lines, binding invariants present), NOT top-level. Audit had guessed top-level path while everything was MISSING. Substantive requirement satisfied. |
| `AGENTS-MACBUILD.md`                              | PRESENT  | (was already present) |
| `macos-port-kickoff.md`                           | PRESENT  | (was already present) |
| `docs/research/macos-implementation-notes.md`    | PRESENT  | (was already present) |
| `docs/adr/`                                       | PRESENT  | 54 entries |
| `docs/phases/`                                    | PRESENT  | 30 entries |
| `docs/judges/`                                    | PRESENT  | 10 entries |
| `experimental/kg-validation/`                    | PRESENT  | full harness tree (5902 entries) |

### Re-audit counts
- REQUIRED satisfied: 12 / 12 (AGENTS.md relocated to `.code_puppy/AGENTS.md`)
- N/A (optional, superseded): 1 (`hooks.toml` -> `settings.json`)
- STOP-worthy MISSING: **0**

**Result: GREEN.** No required carry item is genuinely missing. Two path
deviations from the original guess (AGENTS.md location; hooks.toml ->
settings.json) are documented and non-blocking.

---

## Original audit (pre-carry, everything MISSING)

All listed items are gitignored carry docs / config that must be
hand-carried from a Windows host onto this Mac before judges-seed
(Leaf 0.3) and goal-install (Leaf 0.6) can run.

## Table

| Item                                             | Status   | Notes |
|--------------------------------------------------|----------|-------|
| `.code_puppy/`                                   | MISSING  | entire directory absent |
| `.code_puppy/judges-template.json`               | MISSING  | blocks Leaf 0.3 (judges-seed-mac) at start |
| `.code_puppy/hooks.toml`                         | MISSING  | optional but expected for tool-call hook layer |
| `PLAN-mockingbird-v2.md`                         | MISSING  | top-level planning doc |
| `STATUS.md`                                      | MISSING  | rolling-status doc; goal pinning target in Leaf 0.6 |
| `AGENTS.md`                                      | MISSING  | (note: `AGENTS-MACBUILD.md` is present, see below) |
| `AGENTS-MACBUILD.md`                             | PRESENT  | macOS-specific carry rules |
| `macos-port-kickoff.md`                          | PRESENT  | phased plan source for the port |
| `docs/research/macos-implementation-notes.md`    | PRESENT  | spec for P3/P4 native bindings |
| `docs/adr/`                                      | MISSING  | needed for the Phase 3a Keychain ADR and the Phase 4b SCK ADR |
| `docs/phases/`                                   | MISSING  | per-phase carry notes |
| `docs/judges/`                                   | MISSING  | judges-doc tree |
| `experimental/kg-validation/`                    | MISSING  | KG-deferred but the carry tree is referenced by planning material |

## Counts

- PRESENT: 3
- MISSING: 10

## Implications

- **Leaf 0.3 (judges-seed-mac) STOPPED at start.** Without
  `.code_puppy/judges-template.json` we cannot author a `seed-judges.sh`
  with a meaningful merge target. Per the locked rule, we do not invent
  template content from scratch.
- **Leaf 0.6 (goal install) deferred.** The `mac-v1` persistent goal
  text from drawer 48 is ready to install, but the install medium
  (`~/.code_puppy/goals.json` / `STATUS.md` pin / `bd` epic note) needs
  `.code_puppy/` to exist and `STATUS.md` to be present for the pin.
  Once those land, the goal install is a 5-minute job.
- **`docs/adr/` absence** is downstream of Phase 3a/4b, not Phase 0 —
  but the planner will want the directory present and at least one ADR
  template hand-carried before Phase 3 starts.

## Action required (planner / human)

Hand-carry the following from a Windows host (or restore from a
trusted backup) before the next agent session can resume Phase 0
Leaves 0.3 and 0.6:

1. `.code_puppy/` (whole directory, including `judges-template.json`
   and any existing `hooks.toml`).
2. `PLAN-mockingbird-v2.md`.
3. `STATUS.md`.
4. `AGENTS.md` (top-level — distinct from the present `AGENTS-MACBUILD.md`).
5. `docs/adr/` (whole tree).
6. `docs/phases/` (whole tree).
7. `docs/judges/` (whole tree).
8. `experimental/kg-validation/` (whole tree).
