; Mockingbird UI npm config.
;
; Standing rule: no `npm install`/`npm ci` may execute lifecycle
; scripts. This applies to ALL dependency installs in this project.
; See AGENTS.md.
ignore-scripts=true

; Use the locked lockfile resolutions only.
audit=false
fund=false

; Hide promotional noise + speed up CI.
update-notifier=false
